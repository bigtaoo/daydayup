# DayDayUp

A 2D top-down (3/4 view) free-shooter roguelite, inspired by Soul Knight. The core of the game is a **rich weapon system** — ranged and melee, where melee weapons can block and deflect bullets. Different characters are only skins.

## Structure

An npm workspace. Every package is consumed as TypeScript **source** — there is no
build step between them, so a shared type can never go stale against a built artifact.

| Directory | Contents |
|-----------|----------|
| `design/` | Design and technical decisions (architecture, data models, rationale) |
| `art/` | Art assets and asset conventions |
| `engine/` | `@dd/engine` — the deterministic simulation core (`design/06`, `design/08`). Renderer- and host-free: it compiles without the DOM lib and may not import any other package. Consumed by client, server and both tools. See `engine/README.md`. |
| `client/` | Game client. Single-engine PixiJS v8. Targets: Web / PC / Android / iOS / WeChat mini-game. See `client/README.md`. |
| `server/` | Co-op backend: the frame-broadcast **gameserver** (WebSocket data plane) + the **matchsvc** matchmaking/ticket control plane. See `server/README.md`. |
| `tools/` | Authoring tools, none of them shipped to players. `animator` — the `.tao` rig/clip editor (`design/12`); its `projects/*.editortao` files are the authoring source for every rig in the game. `map-editor` — arena/room authoring, the authority on the `ArenaMap` layout the engine only consumes (`design/15`). `png-pipeline` — a dependency-free pure-Node PNG codec (`pngCodec.mjs`) plus the `compress.mjs` trim/downsample/re-encode step every asset in `client/public/` went through. `desktop-shell` — an Electron app hosting `animator` and `map-editor` as switchable pages in one window, with a native file I/O bridge (`window.nwDesktop.fs`) so Save/Load use real OS dialogs instead of the browser File System Access API. |
| `world/` | Authored world data the tools produce and the engine consumes — today `arenas/arena_prototype_60.json`, the validated 60-room PvP map wired into `ARENA_CATALOG` |

`tsconfig.base.json` and `build/ddAlias.mjs` are the type-side and bundler-side halves
of one `@dd/*` path map — kept as a single file each so they cannot drift apart.

## Tech stack (summary)

- **Single engine: PixiJS v8** (UI included). No Three.js — the game is 2D top-down; the 3D feel is faked with Y-sorting, height/shadow separation, and a tilted view.
- **Fully open source (MIT)** so any issue can be patched or forked. This is the key trade-off versus closed-editor engines like Cocos Creator.
- **WeChat mini-game is the most constrained target** (no DOM, no `fetch`, no WebGPU → WebGL fallback, base-library version must be verified on a real device). It does *not* use `weapp-adapter` — the bundle installs Pixi's own `DOMAdapter`. As of 2026-08-25 real art loads there and the game boots, renders, is tappable, is navigable end to end, enters the map and shows readable text in the real simulator (verified: `Game.start()` runs, the render loop produces real on-screen content, a real PLAY-button tap drives a real screen transition, the menus FIT the 844x390 landscape-phone viewport instead of overflowing it, room building no longer throws on a canvas Pixi cannot identify, and labels rasterise instead of coming out blank) — real-device verification (frame rate, touch feel, lowest base-library version) is what remains. See `design/04-wechat.md`.

See [design/](design/) for the full record.

## Getting started

Install once at the repo root — it is a workspace, so this covers every package.

```bash
npm install
```

```bash
npm run dev        # client dev server, http://localhost:5173
```

| Command | What it does |
|---------|--------------|
| `npm run check` | Typecheck **and** test every package — the one command to run before committing |
| `npm test` | Test every package |
| `npm run typecheck` | `tsc --noEmit` in every package |
| `npm run dev:server` / `npm run dev:matchsvc` | Co-op gameserver / matchmaking service |
| `npm run dev:animator` / `npm run dev:map-editor` | The two authoring tools |
| `npm run dev:desktop-shell` | The Electron shell hosting both authoring tools (run the two `dev:` commands above first, in separate terminals) |
| `npm run test:pvp-sim` | The offline PvP balance harness (`client/sim/`, kept out of the default test glob — ~6s) |
| `npm run test:pve-sim` | The PvE level simulator — bot-driven real runs of level 1, per-room reaction window / peak shooters / clear rate, plus the difficulty gates (`client/sim/`, also out of the default glob) |

## Deployment

The client is live at **https://b.gamestao.com** — static assets (`client/dist`) served
by a Cloudflare Worker (`daydayup-client`, `wrangler/client.jsonc`), same Cloudflare
account as the `funny` project's `gamestao.com` zone.

`.github/workflows/client-deploy.yml` auto-deploys on every push to `main` that touches
`client/**` or `engine/**` (the client build reads the engine as source via the vite
alias), or via manual `workflow_dispatch`. Gated behind repo variable
`CLIENT_DEPLOY_ENABLED` and repo secrets `CLOUDFLARE_API_TOKEN` /
`CLOUDFLARE_ACCOUNT_ID`.

Manual deploy (fallback, or first-time setup):

```bash
npm run build -w client
npx wrangler deploy -c wrangler/client.jsonc
```

An already-open tab picks up a deploy on its own. The client build emits
`dist/version.json` (`build/versionManifestPlugin.mjs` — a hash over the bundle *and*
`client/public/`, so an art-only deploy counts too), and
`client/src/platform/web/autoReload.ts` re-checks it whenever the player returns to the tab,
reloading when it no longer matches the hash the page booted with — held back while a run or
an online session is live, then applied on the next return. `client/public/_headers` keeps
`index.html`/`version.json` uncached at the edge, which is what makes the check meaningful;
don't drop it. Production builds only — the dev server keeps using Vite HMR. Ported from
`funny`'s equivalent; see `design/ROADMAP.md`'s 2026-08-15 entry for how the two differ.

The two authoring tools have the same setup — `wrangler/animator.jsonc` /
`wrangler/map-editor.jsonc` + `.github/workflows/animator-deploy.yml` /
`map-editor-deploy.yml`, gated behind `ANIMATOR_DEPLOY_ENABLED` /
`MAP_EDITOR_DEPLOY_ENABLED` — but those repo variables are **not yet set**, so neither
workflow deploys until that's turned on. Once live they'd serve at
`dd-animator.gamestao.com` / `dd-map.gamestao.com`, which is what the desktop shell's
`tools/desktop-shell/src/tools.ts` `prodUrl` fields point to for a packaged install.

## Status

- [x] Project structure
- [x] Design docs
- [x] Client vertical slice (tilted view / Y-sort / height & shadow / weapon swap / melee block & deflect) — runs in the browser
- [x] Deterministic engine + full PvE loop (floors → extraction → bank), meta/forge, 3-character roster (Phases 0–2)
- [x] Online co-op (Phase 3): frame-broadcast netcode, downed/revive, matchmaking + signed tickets, local-player prediction — two-tab byte-identical lockstep
- [x] PvP (Phase 4): 8-player solo-or-squad battle royale, team/hostility model, real 60-room arena map, shrinking zone, placement win condition, anti-cheat checkpoints, matchsvc Elo ladder — see `design/15-pvp-arena.md`
- [x] PvP squads (design/05/15 follow-up): pre-formed party invite codes, squad-chunked matchmaking/teamId, gated bandage revive, party lobby UI
- [x] Art pipeline (Phase 5): `.tao` rig runtime, full 3-character + boss/critter/brute/floater roster art, a distinct sprite for every ranged/melee weapon id, per-element biome floor/wall art (including the standing walls' front elevation, 2026-08-18 — before that every wall was drawn flat on its own footprint and only the pillars had any height; a same-day follow-up made EVERY wall stand, at three heights, since the first rule's "east-west runs only" exclusion disqualified almost all of the shipped level-1 content and left rooms still reading flat, and added the shading that geometry alone does not give — cap/face/side tonal separation, per-wall ground cast shadows, a dark silhouette, and hand-toned stone pillars off the pre-art palette hues), hub background + button icons + the Forger NPC, UI widget kit, post-processing/particles, all five fidelity-roadmap shaders — dynamic per-pixel lighting (shader-derived fake normal, no normal-map asset needed; re-shaped 2026-08-24 from a filter on EVERY actor into ONE screen-space pass over the scene layer, which cut render p50 from 10.4ms to 2.4ms and let a muzzle flash light the floor rather than only the characters standing on it), energy shield, hit-flash outline, dissolve-on-death, heat-haze (`design/01` 5.2/5.4) — GPT-Image-2-generated art is now treated as final production art (`design/12` 5.3, a 2026-08-03 scope decision, not a tooling change); the WeChat target finally LOADS that art as of 2026-08-25 (it had rendered Graphics placeholders exclusively until then, and `client/public` went 13.66 MB → 3.20 MB to fit the 4 MB main package), but WeChat *device* verification is still outstanding. The rig runtime's placement model was wrong until 2026-08-17 (art drawn at each bone's pivot rather than its tip, and rotated by the bone's raw world angle) — every rigged character rendered visibly disassembled while all of the art itself was fine; fixed along with the previously-undrawn energy tethers, and now guarded by `client/src/render/rigComposition.test.ts`'s assembly invariants over every shipped bundle. The facing model got the same treatment on 2026-08-18: the body now turns toward the AIM instead of the movement vector (an orb-core has no lower body to justify the humanoid split it inherited), the eye slides continuously inside the shell along that aim rather than the billboard's four discrete states carrying the whole 360° read, and `design/01`'s per-weapon front/back z-order rule finally runs every frame. A same-day volume pass then added the lighting that facing work cannot produce: a fixed specular highlight and a curved terminator over the body bone (counter-flipped so the key light does not mirror with the character — the eye moving while the highlight stays put is what reads as a sphere), a far-side weapon module that shrinks and darkens rather than only changing layer, a hover whose lift the shadow actually responds to, and one shared 0.62 foreshortening for every round overlay including the shield ring, which had been a perfect screen-space circle — the shield was pulled back OUT of that rule on 2026-08-24 after a user report that it read as a flat hoop threaded through the character: a shadow and an aura are discs on the ground plane and do foreshorten, but a shield is a sphere around the body, and a sphere's silhouette is a circle from every angle. A third pass on 2026-08-19 stopped looking and started MEASURING the frame (`renderer.extract` at zoom 1, sampled per wall entity from the renderer's own geometry), which found what two look-driven passes had missed: a wall cap raised 104 px read DARKER (44) than the floor it stands on (53), so every other height cue was arguing against the most basic one and losing; each room boundary was drawn as two independent blocks because adjacent rooms each author their own wall, putting a lit/dark seam down the middle of one stone mass; the camera clamp had silently cancelled the wall-height frame extension since the day it shipped, so a room's tallest wall never showed its top; the per-wall stone-relief filter cost 10-32 render targets per room for a measured 0.06% difference and is now off; and both the sphere shading and every ground shadow were sized against a rig's DECLARED body radius while the shipped art paints only 0.68-1.00 of it — which had been painting a hard-edged dark disc onto the background around every crystal enemy, recorded in an earlier session as an over-large shadow. Fixed with an additive cap key light, run merging, a per-room floor light pool (the cheap static half of design/01's parked lightmap), and art-derived body radii guarded by a test that re-decodes the real PNGs every run. One wall then took four more reports on 2026-08-19, all of them the same defect: every tonal constant had been measured on an EAST-WEST wall, where the cap is a thin band under a lit coping, and applied unchanged to a north-south run, where the cap is 100% of what you see. The flat additive key light was destroying the swatch's contrast RATIO (2:1 to 1.4:1) and is now the cap drawn a second time in `add` mode; caps tile in world space so an L corner's two blocks share one stone field; the east band no longer paints a grey stripe down a run's whole top; and a deep run now TUCKS under the wall it meets, stopping just below that wall's crown course — the longest unbroken horizontal in a room, and the line the eye identifies a back wall by. The crown line is measured per element, because `wallface_ice.png`'s coping band is a third shorter than fire's — a bug found by the new `client/src/game/scene/wallComposition.test.ts`, which runs the real level-1 floors through the real wall pipeline and decodes every shipped face swatch to check the constant against its art. **The art-direction blanks are closed as of 2026-08-25**, all three of them locked design rules that had shipped half-built rather than optional extras: design/13's element colour-plus-icon pair is a *dual-channel* rule and only the colour channel had ever existed, so the five icon glyphs (flame/snowflake/bolt/skull/gem) now ride on elemental mobs, on each active status aura, in the HUD's damage badge and on a dropped weapon — vectors, not art, and drawn as solid masses because a stroke is invisible at the 3-8 px these render at; the poison biome got its three swatches, so every element in the closed five has real stone (its brief carries a gameplay clause the others do not — a green floor makes poison bullets and poison-tinted mobs camouflage, so "dial the green down" is stated numerically in the prompt and asserted on the shipped pixels); and weapon rarity's overlay spec is locked as **count, not hue** — four of the five rarity hues turned out to already sit on a reserved element hue and `common` white is byte-identical to physical's neutral, so no palette could have separated them, whereas a countable 0-4 marks cannot be confused with a hue at all
- [x] Client performance analysis (`client/src/perf/`, 2026-08-24, ported from the sibling project `funny`): a windowed frame-timing monitor that runs in EVERY session and names which half of the frame (update vs render) a sustained stutter came from, plus `?perf=1` for an on-screen readout and a WebGL draw-call/program-switch probe. It found this client's actual bottleneck the day it landed — a per-actor lighting filter costing one render-target pass per character (render p50 10.4ms -> 2.4ms once lighting became one screen-space pass). A same-day follow-up added `drawAttribution.ts`, which attributes draw calls to actual objects rather than counting them, and used it to take the frame from 165 draw calls to 108: the wall cap's additive key light is now pre-multiplied into its swatch (a blend-mode change breaks Pixi's sprite batch on both sides), and the structurally-static layers get their own render groups so an actor's per-frame `zIndex` write stops discarding the floor's and the shadows' cached draw instructions. Both verified pixel-identical by GL frame read-back (0 of 1,641,600 pixels). Forcing the same batching inside the Y-sorted `entities` layer was measured and REJECTED (+0.7ms for -50 draw calls) — see `client/src/render/staticGraphics.ts` for the rule that came out of it, and `client/src/perf/README.md` for the before/after numbers, the deviations from funny's original, and what is worth attacking next.
- [x] Accounts (Phase 6, `design/16-accounts.md`): real username/password login (SQLite via `node:sqlite`), never required to play — bound to PvP ladder rating and Forge blueprints/materials/loadout; third-party OAuth reserved, not built
- [x] Internationalization (Phase 7, `design/17-i18n.md`): English-canonical `t()` system with compile-time key checking, 8 locales (中文/Deutsch/Français/Español/Polski/Русский/Italiano), every screen migrated, a language-cycle Settings control, first-boot browser-language auto-detection — enum/data-driven values (damage type, weapon kind, rarity) deliberately left untranslated
- [x] Screen-flow completion (design/10 follow-up, 2026-08-03): a boot loading screen; a menu-driven Mode Select (solo PvE / co-op / PvP solo queue / tutorial) replacing PLAY's direct jump into Forge — co-op/PvP were previously boot-flag-only; a real Matchmaking screen (connecting/error/cancel/retry), fixing a latent bug where a dropped connection hung forever with no feedback; a standalone, always-skippable tutorial level (move/aim/fire → weapon-swap → melee-deflect → the extract-vs-descend checkpoint decision)
- [x] Level 1 balance pass (2026-08-17, `ENGINE_VERSION` 41): a headless PvE level simulator (`npm run test:pve-sim`) — bot-driven real runs reporting per-room reaction window / peak simultaneous shooters / clear rate, with difficulty gates — and the rebalance it measured out: a per-room concurrent-fire budget, staggered room wake-up, halved garrisons, renewable shield regen, plus a door-lock softlock it caught. See `design/05` "Room encounter budget"
- [x] Room feel pass (2026-08-17, `ENGINE_VERSION` 42): the camera frames the current room instead of the whole floor and centres the character rather than their feet; bullets are drawn leaving the gun's actual barrel tip (the sim's ground-plane muzzle and the rig's screen-space one sit on parallel lines — corrected on the view, so hit detection stays authoritative); enemies collide with each other again, gained a perception radius inside the existing room-activation gate, and move slower; the shield shimmer breathes instead of strobing. See `design/05` "Room feel pass". Level 1 is measurably easier as a result — re-tightening garrisons is tracked as open work there
- [x] Wall clearance (2026-08-19, `ENGINE_VERSION` 43): the character stops at its own body radius against a wall or a pillar instead of at its 7 px feet circle — from a play report that walking into a corner read as sinking into the stone. New `Actor.solidRadius`, split off `footprintRadius`, which keeps its old value and now covers only actor↔actor push-out (a body overlapping another body reads as a crowd; a body overlapping stone reads as sunk into it). Enemies keep their old clearance, so no mob path or level-1 garrison measurement moves. See `design/07` and `design/ROADMAP.md` "Sunk into the wall"
- [x] Occlusion x-ray (2026-08-20, client-only): a standing wall block or pillar that is currently drawing over the player goes translucent, from a play report that the character *"跑到墙下面去了"* — walked to the north side of an interior block and vanished (measured: the body's rect read luma 78.4 against the cap stone's 77.1, i.e. arithmetically indistinguishable from the wall). No single layer was wrong — a block's art intrudes one wall HEIGHT north of its own footprint onto walkable floor and sorts in front of it, the player's clearance puts them at most 16 px into that band, so a 70 px block covers all 32 px of the drawn body and per-object sorting offers no "partly hidden". The default pass fades the CAP only (fading the whole block was tried on the live frame and reads as a hole in the room), so the face, the silhouette and the ground shadow keep the mass solid; a pillar has no cap/face split and fades its whole shaft, since that is what a character behind it disappears into. A second pass takes the face too where a tall wall on a shallow footprint buries the body below the cap/face fold and a cap fade would achieve nothing — 1.3% of the floor. Then *"加测试"* produced `occlusionCoverage.test.ts`, which sweeps all 97,803 positions the player can legally stand at across the five shipped floors against an independent overlap oracle, and immediately falsified two claims this session had already written into design/01: a perimeter run DOES trigger the x-ray (a wall between two vertically stacked rooms was the *lower* room's 104 px north perimeter, not the upper room's 22 px kerb — so 72 px of that room's floor made the player completely invisible; fixed at the tier the same day, see "Wall tier" below), and a wall's face DOES cover the player on a shallow footprint. Measured over level 1: 5.5% of standable floor made the character 100% invisible before, none of it leaves more than half hidden after (worst case 43.8%). See `design/01-rendering.md` "The occlusion x-ray" and `design/ROADMAP.md`
- [x] Door grid alignment (2026-08-20, `ENGINE_VERSION` 44): a door's passage rect now lands on a whole grid cell, from a geometry audit that the occlusion sweep above prompted — four wall runs in the shipped level-1 content stood **16 px deep** where every other wall on all five floors is a full 32. Not an art bug and not a merge artifact: `DOOR_EDGE_MARGIN_GRID` is 1.5 and the anchor step is `span / 4`, so a drawn passage could land on a half or even a quarter cell, and `carveDoorGaps` — doing exactly the right rect-difference — then cut a correspondingly misaligned hole, leaving whatever remained of the wall run past it fractionally deep. A 16 px footprint under a 104 px-tall perimeter run is the worst case for the standing-wall art (a cap band on a third of the depth every wall tone was measured on) and it is the geometry that made the x-ray need its second, face-fading pass at all. Fixed in three places because any one alone leaves the hole open: the drawn anchor is snapped in a new shared `world/dungeon/doorAnchor.ts` (the copy of that math each of the two placement functions carried verbatim is now the one thing they share, so a fix cannot land on one path and miss the other — the procedural path was the worse of the two, with 33 fractional passages across a 10-seed sweep); level 1's nine fractional `passageGrid` values are floored to whole cells as data, which is what actually removes the four runs from a real run; and the map editor's save gate rejects a hand-typed fractional passage, which it never did even while holding `solids` to whole cells. `DOOR_EDGE_MARGIN_GRID` itself stays 1.5 — it is the fit threshold, and no integer value satisfies both halves of what the tests already pin, which the suite now demonstrates rather than asserts (setting it to 1 fails 5 tests, to 2 fails 6). See `design/05` "Door position is authored freely" and `design/ROADMAP.md` "The 16 px wall runs"
- [x] Wall tier: the boundary between stacked rooms (2026-08-20, client-only): `wallGeometry.wallTier` decided a wall's height from the one room the wall's **centre** falls in, so "am I a south boundary?" could only ever be asked about that one room — and where two rooms stack vertically the boundary between them is *two* walls, one grid row apart, authored by two different rooms. The upper room's own south wall kerbed correctly at 22 px; the lower room's north wall answered "I am my room's north edge" and stood at the full 104 px, one row south of the exact floor the kerb tier exists to keep clear. A block's art rises from its own north edge, so it reached a measured 72 px into the room above — 22 runs of it, on all five shipped floors. This is the *"kerb rule is defeated for any shared boundary"* follow-up the occlusion sweep found and the entry above recorded; the x-ray was keeping the player visible there, which is work a correct tier does not need done. The rule now asks *"does any room's floor lie immediately north of me"*, one predicate covering both halves of a shared boundary, which strictly generalizes the old test (a room's own south wall is the case where the room to the north is the wall's own room). No splitting pass is needed and that is the point: every room authors its own four perimeter walls, so a boundary arrives as two independently-tiered rects and `RoomBuilder` tiers **before** it merges — a collinear north boundary shared by two side-by-side rooms, only one of which has a room above it, therefore keeps its tall half, where the old rule had merged the two into one 32-cell perimeter run. Measured over level 1 with the fix stashed and unstashed: the share of standable floor that leaves the character at least half hidden before the x-ray drops 8.5% → **5.4%**, completely invisible 5.5% → **3.3%**, the deep face-fading pass 1.2% → **0.2%**, and 19 runs move from perimeter to kerb (105 → 86 perimeter, 37 → 53 kerb). Confirmed on a live frame too (luma scan down world x=350 at floor 0's `r4_forge`/`r5_extraction` boundary: stone used to start at y 440 = `544 − 104`, now starts at 492 = `512 − 22`). PvP is untouched — the 60-room arena's rooms all carry `solids: []`, so it has no wall runs at all. See `design/01-rendering.md` "Every wall stands, at one of three heights" and `design/ROADMAP.md` "The wall between two rooms was standing on the wrong floor"
- [x] The occlusion x-ray generalized to every enemy, and a door could still be blocked by a wall (2026-08-20, client-only): the x-ray above only ever built its focus from the local player, so a monster standing in the exact hidden band a wall's art intrudes over — live report *"如果只有怪物在墙下面的话，就看不到怪物了"* — got no x-ray at all and rendered fully swallowed; `occlusion.updateOcclusion` now takes a list of foci (local player + every live enemy), and a block fades if it hides ANY of them. Separately, a door standing immediately north of a wall run has no x-ray to fall back on at all — it renders on `layers.ground`, strictly behind the Y-sorted wall it would need to fade against — so a deep north-south run spilling its cap past its own footprint (the same "art intrudes one wall height north of its footprint" mechanism as the x-ray case) could visibly cover a door (*"门不能被高墙挡住了。门应该是随时清晰可见的"*). Fixed at the geometry instead: `wallRuns.bordersDoorNorth` finds a run whose north edge meets a door passage, and `blockCapTop` clips that run's cap to stop at its own footprint. That clip alone only ever helped a DEEP run, and a sweep of the real shipped pipeline (`doorSpillCoverage.test.ts`) found the SHALLOW shape — the more common one, since an ordinary wall's thickness is almost always shallower than its tier height — still firing 12 times across all five floors: a block's FACE is drawn at a fixed tier height regardless of its own footprint depth, so for a shallow footprint the face alone reached past the run's edge with nothing left for a cap-only clip to touch (measured: 72 px of pure face on a 32 px-deep stub). `wallRuns.effectiveWallHeight` closes it by shrinking the height fed to both the face and the cap. A follow-up pass also removed `LIT_WALLS`, a per-segment stone-relief filter kept switched off since 2026-08-19 (measured at a 0.06% mean frame difference) rather than deleted — dead weight with no re-tune ever queued up, gone outright rather than left as a permanently-false switch. See `design/01-rendering.md` "The occlusion x-ray" and "A north-south run is not an east-west wall", and `design/ROADMAP.md`'s entries for the same date.
- [x] Doors stand up, and the floor stops at its rooms (2026-08-20, client-only): the scene passes so far had gone into the character and the walls, so a floor-wide `renderer.extract` was measured to find what else was wrong — and the biggest thing was that a door was still a flat `Sprite` on `layers.ground`, stretched to its passage rect, while every wall around it stood 22-104 px. `door_{locked,open}_raw.png` are front ELEVATIONS (a portrait stone frame around a hazard slab; the same frame as an empty arch), i.e. the exact mistake `design/13` records for the wall swatches before 2026-08-18, still live for the one fixture a player has to read at a glance — 221x320 of portrait art squeezed into 64x128, and into a 128x64 LANDSCAPE rect for a north-south passage. `scene/doorRender.ts` now builds a door as a wall block whose FACE is an opening: because a passage rect is a hole in a wall, the mass above a doorway lands exactly where a wall block's cap lands and the opening lands where its face goes, so one construction covers both orientations, from the same shell `wallRender.ts` uses (its cap/face/edge builders are now shared, not re-implemented). A door stands as tall as the wall it interrupts and never taller (`wallRuns.doorFlankTier`) — 11 of the 24 doors on the shipped floors are cut into a 22 px kerb, which is low precisely so it cannot stand between the camera and the player, and a doorway there gets its read from an additive hazard bloom instead of from height. The leaf is fit by width and cropped, never squashed (fitting both axes compresses the kerb case 8:1), and both PNGs were re-trimmed through `tools/png-pipeline/compress.mjs` — they carried ~33 px of transparent margin per side, differently on each state, so the leaf covered 67% of its own opening and the two states were not registered against one another. Doors are also x-ray occluders now, which they could not be while they lived on `ground`. **The floor was the second half of the same pass.** It was one `TilingSprite` of a 256 px swatch over the world, with two separate defects: three 48x48 samples 512 px apart in three DIFFERENT rooms came back byte-identical (an exact 8-cell period, the same floor everywhere), and — the bigger one, which the first measurement had not thought to ask about — 29-56% of it was painted where no room exists at all, because `worldW/worldH` is the bounding box of a floor's co-resident rooms and a `graph2d` layout's rooms do not fill their own bounding box (1.41-2.26x their area across the five shipped floors; on floor 0 the surplus is a single featureless 1500x430 field with no walls and no room light in it, which reads as one enormous room the level happens to have wall-lines drawn across). The floor now stops at the rooms, and `floorCoverage.test.ts` proves that is safe by flood-filling every shipped floor from every room centre through non-wall cells and requiring each reachable cell to be inside a room rect — while the same sweep is why a PvP arena deliberately keeps the whole-world floor (5240 of `arena_prototype_60`'s 11,524 non-wall cells are reachable and outside every room rect and passage, so a per-room floor there would put a player on the backdrop). On top of that: a per-room warm/cool wash (room floor means now span 40.6-54.2 luma), mottle at a scale incommensurate with the tile grid (64 px patch-mean sd 2.69 → 4.23), a mirrored per-tile stamp instead of one tiling sprite (mirrors only — a seamless tile stays seamless mirrored and does not rotated), and wear: stains, rubble lit from the same upper-left key light as everything else, and a worn patch across each doorway along its travel axis. Two first attempts were wrong in ways only a 2x crop showed (a hashed per-tile tint painted the tile grid as a checkerboard of flat rectangles; mottle bands at a flat alpha drew visible arcs). `RoomBuilder.ts` crossed 500 lines doing this, so the ground stage moved to `groundLayer.ts` (539 → 424 lines, no behaviour change). A follow-up coverage audit (*"有测试可以加吗"*) widened the pass's mutation battery from 12 mutants to a scripted 21 over every branch and constant in the new code, and **16 of them survived** — the tests written alongside the code, measurements in their comments and all, covered about a quarter of the decisions it makes. All 16 are now dead (1941 tests, +65 for the pass), including the ones that mattered most: the recess and the sill removed entirely, a cropped floor tile ignoring its source offset (a duplicated strip along every room's east and south edge), the mottle's additive half dropped, the grid mounted under the floor, and the per-room seed replaced by an array index. Two needed a second attempt at the TEST rather than the code — an assembly assertion of the form "the fixture contains this geometry" passes trivially when the geometry is empty, and a "two rooms differ" assertion has to compare shapes relative to each room's origin and filtered to the shapes themselves, since Pixi emits its own `moveTo(0, 0)` before each one. The one item this pass measured and did NOT fix — pillars reading as smooth cans beside the stone walls — was closed the same day; see the next entry. See `design/01-rendering.md` "A door is a wall block whose face is an opening" + "The floor stops at its rooms", and `design/ROADMAP.md` "Doors that stand, and a floor that stops at its rooms"
- [x] Pillars get real art (2026-08-20, client-only): the last item on the scene queue, and the one the previous pass measured but left — a pillar's cap was a hand-toned gradient where a wall cap is a real swatch under an additive key light, so four smooth cylinders stood in a room of textured stone. `pillarRender.ts` had already recorded, in 2026-08-18, why the obvious fix does not work: sampling the 256 px WALL swatches through a ~35 px cap window lands on one arbitrary dark patch, and the brick elevation on the shaft reads as an open-topped well. A fixed-size object (`radius: 1` in every shipped room, drawn 84x98) needs art authored at its own scale, so this ships as one whole-object SPRITE — and **one file for every biome, not one per element**: `pillarTint()` multiplies the biome's wall colour over it, the same channel the hand-toned body already used. Seven generations, six rejected, and the rejects carried the lessons: **five were generated at exactly 84x98**, the size the game draws — but `FxController.MAX_ZOOM` is 4.5 and the renderer runs at up to 2x device pixel ratio, and level 1's gallery room actually renders at zoom 4, so a sprite is MAGNIFIED in real play and art at game size is simply a blurred pillar (the one defect that cannot be repaired at import, hence an explicit output resolution in the prompt and a 384 long axis where every swatch in that directory uses 256); one read as an open-topped barrel, exactly the drift the prompt's own negative constraint names; one had a top surface at luma 125, the brightest thing in the room, repeating the defect the 2026-08-19 measured pass fixed; and the real near-miss — a per-element fire attempt that followed the canvas/margin/aspect spec exactly but drew its course joints STRAIGHT (centre-vs-edge sag −2 px against the accepted file's +53) and put 21% of its height into the top surface against the accepted file's 30%, i.e. two files that would ship as two camera angles in a game with one fixed camera, for a difference of 1.8% warm pixels that a tint gets for free. The accepted generation then needed two import steps, neither visible by looking at it: it came back with a transparency **CHECKERBOARD painted in as opaque pixels** (3.46M opaque, zero transparent — the standing "decode the alpha, don't trust the picture" rule in a new costume, since the file did not lack alpha so much as depict it), keyed out by luma and flood-filled from the border to prove no interior holes; and its SHAFT measured twice the value of the wall face beside it (lit limb 71.6 against 27.3) while its top was already on target at 87.3 against wall caps of 72-81 — same stone, same key light, reading as a different material. A uniform multiply cannot fix that, since darkening the shaft drags the on-target top down with it, so new `tools/png-pipeline/lumaCurve.mjs` scales RGB by a gain chosen from each pixel's OWN luma: top untouched, shaft bands 84/59/30 → 57/40/20, which is the same fold between a bright top and a much darker face that `wallTone.ts` builds for the walls, applied once offline instead of as a per-object filter. Measured live after (extract of the real gallery room at zoom 1, sample rects derived from the sprite's own bounds): top 87.3, lit limb 50.4, mid 35.8, dark limb 16.7, foot 25.0, against wall caps 72-81, wall faces 27.3-27.5 and a floor of 48.5 in the same frame. Two would-be silent bugs avoided on the way: the occluder box the x-ray reads is measured off the SPRITE (`pillarArtExtent(bodyW, height, tex)`), never off the hand-toned ellipse maths, and the sprite key loads with `autoGenerateMipmaps: true` and WITHOUT the `addressMode: 'repeat'` every tiling swatch gets — a 326 px source drawn at 84 px is the same 4:1 minification that turned the rig art into colour noise in 2026-08-12, and `repeat` on a lone object samples the far side of its own silhouette. Then *"有测试可以加吗"* twice over: a scripted 21-mutant battery (18 over the code, 3 over the shipped ASSET — revert the curve, apply it uniformly, compress to game size) came back with **3 survivors**, and a second 18-mutant battery over the decisions the first left alone came back with **11**, in three clusters worth naming — the contact crease's GEOMETRY was invisible to tests that only read fill alphas (it could be mounted under the sprite it shades, sized off the art's height instead of the room's, or lose its corner rounding, all green); the biome could be dropped silently now that one file serves every element (`getPillarTexture('neutral')` hardcoded, or the sprite tinted from the neutral palette); and three `lumaCurve` mutants survived tests written for exactly those properties because the FIXTURE made the mutation equivalent — a bright transparent pixel is skipped by the gain short-circuit anyway, a hard step still comes out monotonic, and two pixels whose red channel and luma fall on the same side of the band cannot tell a luma-keyed gain from a channel-keyed one. All 14 closed; both batteries now kill everything (21/21 and 18/18). 1968 client tests + 30 in `png-pipeline` (the tool had none — `applyLumaCurve` was extracted from its CLI to be testable, and two tests drive the real CLI, because the entry-point gate that decides whether the pipeline step does anything at all is itself a decision). New `pillarArt.test.ts` decodes the SHIPPED PNG and measures it — real alpha with transparent corners, resolution headroom, the aspect the `WALL_HEIGHT` agreement depends on, a top that is the brightest plane, a shaft in the wall face's family, no baked base darkening — because the pillar's whole look now lives in a binary file, where no test of the code can see it. See `design/01-rendering.md` "A pillar is a sprite now", `art/biome/prompts.md` "Pillar SPRITE", and `design/ROADMAP.md` "Pillars get real art"
- [ ] WeChat mini-game adaptation (see `design/04-wechat.md`) — the adapter, the real-art loading path and the 4 MB package budget are done, gated in CI, and confirmed in the real simulator; **2026-08-25: five WeChat-only bugs found and fixed, each one hiding the next** — `Game`'s constructor threw (`URLSearchParams` missing on that runtime, not the previously-suspected shader) so boot never reached `Game.start()`, every menu `Button`/`Slider` was silently unclickable (Pixi's interaction system never receives anything on a canvas with no real DOM event API — the in-run twin-stick controls were unaffected, since they bypass Pixi's interaction system entirely), and then, once tapping worked, the Forge was a dead end because every menu screen is laid out for a viewport about twice as tall as a landscape phone's 844x390 (fixed by a fit-scale on the menu layer, `client/src/game/ui/menuLayer.ts`, swept over every screen at five viewports), then entering any room threw because `capLight` built its baked wall-cap texture with `Texture.from(canvas)`, which identifies a canvas by `instanceof` against DOM globals this runtime lacks, and finally every label in the game rendered blank because Pixi sets `context.letterSpacing = '0px'` before every measurement and every `fillText` and that one assignment poisons a wx 2D context (`measureText` goes non-finite, `fillText` paints nothing — bisected in the running mini-game at 1058 painted pixels without the step and 0 with it). The last two are the same class as each other: Pixi identifying a browser by touching a DOM global, on a runtime that has some of them and not others — and note that the DevTools simulator DOES provide `document`, which is what let browser-only code look healthy there. What remains: real-device verification (frame rate, touch feel, lowest base-library version)
- [x] Room props get real art, and three loaders that were never mip-mapped (2026-08-24, client-only): the last Graphics placeholder in a level-1 room, taken on because the user asked to **validate the level's art and the client's performance before more feature work** rather than discover a shortfall after. Three sprites from four generations (`prop_{crate,barrel,rubble}.png`), and the reject is the interesting one: the first rubble came back as pale diagonal splinters, median luma 61 against the ember floor's 34, reading as bone chips on dark stone — backwards from design/13's "environment desaturated, hazards saturated", where a light heap reads as loot. Its TONE was fixable offline with `lumaCurve.mjs`; its silhouette was not, which is what decided the reroll, and the reroll worked by naming the primitive with a count ("BLOCKS, not shards"), stating the value target numerically (median ~45, highlights <=100 — it landed 48/95) and naming what a wrong value would be mistaken for. **A new alpha defect class came with them**: all three decoded as textbook bimodal alpha and were not, carrying a veil of alpha 1-10 reaching 50-140 px past the object with the body at 252-253 rather than 255. Both ends are invisible (4% and 99% opacity) and `alpha-audit.mjs` reads the pair as one "suspicious" file with no opaque pixels at all — but `trimAlphaBoundingBox` keeps any pixel with `alpha !== 0`, so the veil became part of the object: the rubble trimmed to aspect 2.95 against its real 3.67, and since a prop is scaled by WIDTH with the art's aspect setting its height it would have stood 25% too tall, above 123 empty rows that a bottom-anchored sprite turns into clearance off the floor. New `tools/png-pipeline/alphaClamp.mjs` snaps both plateaus and runs BEFORE `compress.mjs`; every file shipped before this measures identically at `alpha > 0` and `alpha > 25`, so the `alpha !== 0` trim had simply never been handed a file where it mattered. Landing the art also exposed a live integration gap — `buildPropBody` took a `BiomePalette` and, on the sprite branch, never read it, so `PROP_BIOME_MIX` stopped applying to anything the player could see the moment real art arrived (new `propTint`, the same mix-into-white trick `pillarTint` uses; it deliberately does NOT re-hue the crate, whose warm lean is wood and whose separation from the loot crate is VALUE, 53 against 167, not hue). Content went from 15 placements across 8 of 14 pieces to 54 across all 14, swept by `propContent.test.ts` against solids, both spawn kinds, the door band and each other — a validator written BEFORE the content, which rejected nine of the first placements. Then two findings that were not about props at all: verifying the props' mip chains meant dumping every texture in a live frame, and **`weaponSkins.ts` and `uiSkins.ts` were still passing a bare url to `Assets.load`** — a mounted weapon measures 5.3:1 minification in a real room (320 px source, 60 px on screen), worse than the 4:1 that made the pillar need a mip chain, on the object every actor in the frame carries; and design/12's last facing gap (a character facing away still showing its front belly chamber) closed with **no new art** via a `FRONT_ONLY_BONES` set gated on there being no `__back` texture, so a future `belly__back` PNG supersedes it for free. Tooling: `perf/frameProbe.ts` packages the A/B/C frame diff every art pass had been retyping, with the guard this one paid for — an hour went into diffs that all read zero because the scene under test was never on screen (`Game.beginRun()` is callable from the console but does not hide the main menu, which draws over everything in `layers.ui`), and **the restore check cannot catch that**, since hiding something invisible changes nothing and C equals A. `probeFrames` therefore requires a liveness control, runs it first, and reports `trustworthy: false` when it moves zero pixels. Client performance is **not** validated and the reason is recorded rather than papered over: this machine's Chrome reports `Microsoft Basic Render Driver` — a software rasterizer with no `EXT_disjoint_timer_query_webgl2` — so real GPU time is unmeasurable here. One result survived two runs (removing the `fx` layer's `BlurFilter({strength:3, quality:2})`, 4 of the frame's ~7 full-screen passes, roughly halves the cost at 390x844 DPR 3) and the rest of the attribution did not, since removing ALL filters measured slower than removing one, which is physically impossible. A 加测试 pass then ran a 38-source-mutant plus 12-content-mutant battery: **27/38** first run, and the nine real gaps were all in the newest code, in two recognisable shapes — every `frameProbe` fixture was the same kind of degenerate (all grey, so swapped Rec.601 coefficients agreed; all non-negative coordinates, so the lower clamp was never walked; all ascending scan order, so the percentile sort was invisible), and the DEFAULT path had no test because every test passed an override (`defaultControl`'s undo could have been a no-op, permanently destroying the scene it measures; the `probe` handle forwarded its caller's callbacks and replacing them with no-ops survived the whole suite). `readFrame`/`frameRectOf` had zero coverage on the grounds that this workspace has no DOM, which `vi.stubGlobal('document', ...)` disposed of. Content mutants killed 12/12 first run; second source run 37/38, the survivor being a genuinely equivalent mutant recorded as such rather than chased. 4220 tests. See `design/ROADMAP.md` "Room props get real art", `art/props/prompts.md`, and `client/src/perf/README.md`
- [x] The shield becomes a shell, and a shader you can finally measure (2026-08-25, client-only): a user report with a red circle drawn round the hero — *"现在的护盾是一个圆圈包裹着角色，我希望的是类似一个透明的蛋壳一样的效果将角色全部包裹，而不是一个圆环"*. The 2026-08-24 fix above had un-squashed this shield back into a true circle, which fixed the ellipse and left the OTHER half of 圆圈 standing: the shader still drew `smoothstep(a, b, dist) * (1.0 - smoothstep(b, c, dist))`, a band with a hole in it, so a shielded character stood in empty space with a hoop round its waist — a ring is not a small shell, it is a different object. `EnergyShieldFilter` now paints a glass sphere: a filled interior (the one thing a ring cannot do — every previous version computed exactly zero at the centre), a Fresnel limb off a sphere normal (`pow(1 - nz, 3)`; linear washes the whole disc into a flat cyan blob, which is what reads as a decal), one specular glint at ~0.6 of the radius, and a radius grown 1.55 → 1.87 body radii because 全部包裹 includes the mounted weapon, whose barrels had been sticking out through the shell. Two of those numbers came from LOOKING at a real frame rather than at the source: the glint at 0.45 of the radius landed on the body, where an additive highlight over near-white character art is invisible, and the interior fill had to be damped by the body's own alpha (`FILL * (1.0 - 0.55 * color.a)`) because undamped it flattened the hero's face — the saturated blue eye came out the same pale cyan as the shell around it. The shell still stops short of the feet, so the ground shadow the 2026-08-19 volume pass added reads through its ~8% interior alpha. Then *"可以加测试吗"*, and the answer needed a different KIND of test: the existing shield suite reads the shipped GLSL as text, which is the wrong evidence for a question about a shape, since two shaders can satisfy every regex while painting completely different things. There is no GL context under vitest on this machine and a TS reimplementation of the equation (the approach `sceneLightModel.test.ts` takes, correct for a lighting formula) cannot catch a STRUCTURAL regression — it would go on computing a shell while the shader drew a ring. So `shieldShellModel.test.ts` interprets the real thing: a GLSL-subset evaluator (tokenizer, Pratt parser, GLSL broadcasting, the builtins this shader uses) executes the shipped `main`, prelude functions and `frameUv`'s pow2 remap included, duplicating no constant and no formula, and throwing rather than skipping on anything it cannot evaluate. 25 tests over the shape (centre painted, no hole out to the surface, circular at 16 angles, gone past the fade, a glint off-centre and inside the shell), the transparency, the fade with the shield pool and the breathing pulse, and the region-centring across four region/pow2-texture mismatches — the crescent bug that took this filter three separate fixes is a CASE now, not a comment — plus three tests on the evaluator itself, since one that quietly mis-evaluates makes every measurement agree with anything. Running it found two things reading the source had not: the composite is not monotonic (the shimmer bands radially, so a ray outward ripples ~0.03% of peak — pinned as a magnitude, because a ring dips 100%), and an alpha bound read at `uTime = 0` is a lucky sample that the `glow * 0.7` composite knob mutated to 1.0 walked straight through. 16 mutants, **16 killed by the measured suite alone**, and the split with the source-contract suite is informative rather than redundant: the regex suite misses "`uIntensity` ignored" entirely, and catches a dropped `frameUv` with one failing test where the measured suite catches it with fifteen. See `design/01-rendering.md` "The shield is the deliberate exception" and `design/ROADMAP.md` "The shield becomes a shell"
