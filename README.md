# DayDayUp

A 2D top-down (3/4 view) free-shooter roguelite, inspired by Soul Knight. The core of the game is a **rich weapon system** — ranged and melee, where melee weapons can block and deflect bullets. Different characters are only skins.

## Structure

An npm workspace. Every package is consumed as TypeScript **source** — there is no
build step between them, so a shared type can never go stale against a built artifact.

| Directory | Contents |
|-----------|----------|
| `design/` | Design and technical decisions (architecture, data models, rationale) |
| `art/` | Art assets and asset conventions |
| `engine/` | `@dd/engine` — the deterministic simulation core (`design/06`, `design/08`). Renderer- and host-free: it compiles without the DOM lib and may not import any other package. Consumed by client, server and both tools. See `engine/README.md`. |
| `client/` | Game client. Single-engine PixiJS v8. Targets: Web / PC / Android / iOS / WeChat mini-game. See `client/README.md`. |
| `server/` | Co-op backend: the frame-broadcast **gameserver** (WebSocket data plane) + the **matchsvc** matchmaking/ticket control plane. See `server/README.md`. |
| `tools/` | Authoring tools, none of them shipped to players. `animator` — the `.tao` rig/clip editor (`design/12`); its `projects/*.editortao` files are the authoring source for every rig in the game. `map-editor` — arena/room authoring, the authority on the `ArenaMap` layout the engine only consumes (`design/15`). `png-pipeline` — a dependency-free pure-Node PNG codec (`pngCodec.mjs`) plus the `compress.mjs` trim/downsample/re-encode step every asset in `client/public/` went through. `desktop-shell` — an Electron app hosting `animator` and `map-editor` as switchable pages in one window, with a native file I/O bridge (`window.nwDesktop.fs`) so Save/Load use real OS dialogs instead of the browser File System Access API. |
| `world/` | Authored world data in JSON form — today only `dungeons/`, the PvE level-1 room library and floor maps the map editor round-trips. PvP's launch map is TypeScript content instead: `engine/world/arenas/` (`arena_launch`, "The Seven Districts") |

`tsconfig.base.json` and `build/ddAlias.mjs` are the type-side and bundler-side halves
of one `@dd/*` path map — kept as a single file each so they cannot drift apart.

## Tech stack (summary)

- **Single engine: PixiJS v8** (UI included). No Three.js — the game is 2D top-down; the 3D feel is faked with Y-sorting, height/shadow separation, and a tilted view.
- **Fully open source (MIT)** so any issue can be patched or forked. This is the key trade-off versus closed-editor engines like Cocos Creator.
- **WeChat mini-game is the most constrained target** (no DOM, no `fetch`, no WebGPU → WebGL fallback, base-library version must be verified on a real device). It does *not* use `weapp-adapter` — the bundle installs Pixi's own `DOMAdapter`. As of 2026-08-25 real art loads there and the game boots, renders, is tappable, is navigable end to end, enters the map and shows readable text in the real simulator (verified: `Game.start()` runs, the render loop produces real on-screen content, a real PLAY-button tap drives a real screen transition, the menus FIT the 844x390 landscape-phone viewport instead of overflowing it, room building no longer throws on a canvas Pixi cannot identify, and labels rasterise instead of coming out blank) — and the five custom shaders are confirmed to COMPILE and do real work there, measured one pass at a time against a live room rather than inferred from "the frame is non-blank". Real-device verification (frame rate, touch feel, lowest base-library version) is what remains, and it now has something to act on: `render/quality.ts` adds `auto`/`high`/`low` render tiers that turn off all four full-viewport filter passes and halve the renderer resolution (render-target switches 11 → 1), with an `auto` watchdog that drops a device that cannot hold the frame — so a low-end handset reports itself in the settings screen as `AUTO (LOW)`. See `design/04-wechat.md`'s **On-device test plan**.

See [design/](design/) for the full record.

## Getting started

Install once at the repo root — it is a workspace, so this covers every package.

```bash
npm install
```

```bash
npm run dev        # client dev server, http://localhost:5173
```

| Command | What it does |
|---------|--------------|
| `npm run check` | Typecheck **and** test every package — the one command to run before committing |
| `npm test` | Test every package |
| `npm run typecheck` | `tsc --noEmit` in every package |
| `npm run dev:server` / `npm run dev:matchsvc` | Co-op gameserver / matchmaking service |
| `npm run dev:animator` / `npm run dev:map-editor` | The two authoring tools |
| `npm run dev:desktop-shell` | The Electron shell hosting both authoring tools (run the two `dev:` commands above first, in separate terminals) |
| `npm run test:pvp-sim` | The offline PvP balance harness (`client/sim/`, kept out of the default test glob — ~6s) |
| `npm run test:pve-sim` | The PvE level simulator — bot-driven real runs of level 1, per-room reaction window / peak shooters / clear rate, plus the difficulty gates (`client/sim/`, also out of the default glob) |
| `DD_REPLAY=<path> npm run replay:inspect` | Read one recorded run (a `ddreplay-*.json` a player saved with the HUD's ● button or F9) and report every drop's closest approach, swept path, gate and `pickup` event (`client/sim/replay/`) |

## Deployment

The client is live at **https://b.gamestao.com** — static assets (`client/dist`) served
by a Cloudflare Worker (`daydayup-client`, `wrangler/client.jsonc`), same Cloudflare
account as the `funny` project's `gamestao.com` zone.

`.github/workflows/client-deploy.yml` auto-deploys on every push to `main` that touches
`client/**` or `engine/**` (the client build reads the engine as source via the vite
alias), or via manual `workflow_dispatch`. Gated behind repo variable
`CLIENT_DEPLOY_ENABLED` and repo secrets `CLOUDFLARE_API_TOKEN` /
`CLOUDFLARE_ACCOUNT_ID`.

Manual deploy (fallback, or first-time setup):

```bash
npm run build -w client
npx wrangler deploy -c wrangler/client.jsonc
```

An already-open tab picks up a deploy on its own. The client build emits
`dist/version.json` (`build/versionManifestPlugin.mjs` — a hash over the bundle *and*
`client/public/`, so an art-only deploy counts too), and
`client/src/platform/web/autoReload.ts` re-checks it whenever the player returns to the tab,
reloading when it no longer matches the hash the page booted with — held back while a run or
an online session is live, then applied on the next return. `client/public/_headers` keeps
`index.html`/`version.json` uncached at the edge, which is what makes the check meaningful;
don't drop it. Production builds only — the dev server keeps using Vite HMR. Ported from
`funny`'s equivalent; see `design/ROADMAP.md`'s 2026-08-15 entry for how the two differ.

The two authoring tools have the same setup — `wrangler/animator.jsonc` /
`wrangler/map-editor.jsonc` + `.github/workflows/animator-deploy.yml` /
`map-editor-deploy.yml`, gated behind `ANIMATOR_DEPLOY_ENABLED` /
`MAP_EDITOR_DEPLOY_ENABLED` — but those repo variables are **not yet set**, so neither
workflow deploys until that's turned on. Once live they'd serve at
`dd-animator.gamestao.com` / `dd-map.gamestao.com`, which is what the desktop shell's
`tools/desktop-shell/src/tools.ts` `prodUrl` fields point to for a packaged install.

## Status

- [x] Project structure
- [x] Design docs
- [x] Client vertical slice (tilted view / Y-sort / height & shadow / weapon swap / melee block & deflect) — runs in the browser
- [x] Deterministic engine + full PvE loop (floors → extraction → bank), meta/forge, 3-character roster (Phases 0–2)
- [x] Online co-op (Phase 3): frame-broadcast netcode, downed/revive, matchmaking + signed tickets, local-player prediction — two-tab byte-identical lockstep
- [x] PvP (Phase 4): 8-player solo-or-squad battle royale, team/hostility model, a hand-authored 60-room arena map (`arena_launch`, seven districts joined by twelve arteries — `engine/world/arenas/`), shrinking zone, placement win condition, anti-cheat checkpoints, matchsvc Elo ladder — see `design/15-pvp-arena.md`
- [x] PvP squads (design/05/15 follow-up): pre-formed party invite codes, squad-chunked matchmaking/teamId, gated bandage revive, party lobby UI
- [x] Art pipeline (Phase 5): `.tao` rig runtime, full 3-character + boss/critter/brute/floater roster art, a distinct sprite for every ranged/melee weapon id, per-element biome floor/wall art (including the standing walls' front elevation, 2026-08-18 — before that every wall was drawn flat on its own footprint and only the pillars had any height; a same-day follow-up made EVERY wall stand, at three heights, since the first rule's "east-west runs only" exclusion disqualified almost all of the shipped level-1 content and left rooms still reading flat, and added the shading that geometry alone does not give — cap/face/side tonal separation, per-wall ground cast shadows, a dark silhouette, and hand-toned stone pillars off the pre-art palette hues), hub background + button icons + the Forger NPC, UI widget kit, post-processing/particles, all five fidelity-roadmap shaders — dynamic per-pixel lighting (shader-derived fake normal, no normal-map asset needed; re-shaped 2026-08-24 from a filter on EVERY actor into ONE screen-space pass over the scene layer, which cut render p50 from 10.4ms to 2.4ms and let a muzzle flash light the floor rather than only the characters standing on it), energy shield, hit-flash outline, dissolve-on-death, heat-haze (`design/01` 5.2/5.4) — GPT-Image-2-generated art is now treated as final production art (`design/12` 5.3, a 2026-08-03 scope decision, not a tooling change); the WeChat target finally LOADS that art as of 2026-08-25 (it had rendered Graphics placeholders exclusively until then, and `client/public` went 13.66 MB → 3.20 MB to fit the 4 MB main package), but WeChat *device* verification is still outstanding. The rig runtime's placement model was wrong until 2026-08-17 (art drawn at each bone's pivot rather than its tip, and rotated by the bone's raw world angle) — every rigged character rendered visibly disassembled while all of the art itself was fine; fixed along with the previously-undrawn energy tethers, and now guarded by `client/src/render/rigComposition.test.ts`'s assembly invariants over every shipped bundle. The facing model got the same treatment on 2026-08-18: the body now turns toward the AIM instead of the movement vector (an orb-core has no lower body to justify the humanoid split it inherited), the eye slides continuously inside the shell along that aim rather than the billboard's four discrete states carrying the whole 360° read, and `design/01`'s per-weapon front/back z-order rule finally runs every frame. A same-day volume pass then added the lighting that facing work cannot produce: a fixed specular highlight and a curved terminator over the body bone (counter-flipped so the key light does not mirror with the character — the eye moving while the highlight stays put is what reads as a sphere), a far-side weapon module that shrinks and darkens rather than only changing layer, a hover whose lift the shadow actually responds to, and one shared 0.62 foreshortening for every round overlay including the shield ring, which had been a perfect screen-space circle — the shield was pulled back OUT of that rule on 2026-08-24 after a user report that it read as a flat hoop threaded through the character: a shadow and an aura are discs on the ground plane and do foreshorten, but a shield is a sphere around the body, not a disc under it. The circle that fix landed on was itself wrong, and stood until 2026-08-27: *"a sphere's silhouette is a circle from every angle"* is true of a real camera, and this renderer is not one — it draws the world grid unsquashed while extruding wall heights 1:1 upward in px, i.e. the shear `(x, y, z) -> (x, y - z)`, under which a sphere projects to an ellipse with semi-axes 1 and sqrt(2). The shell is 1.30 taller than wide now, and the two constants are the two halves of how round things read here: squashed = lying on the ground, taller = standing in the world. A third pass on 2026-08-19 stopped looking and started MEASURING the frame (`renderer.extract` at zoom 1, sampled per wall entity from the renderer's own geometry), which found what two look-driven passes had missed: a wall cap raised 104 px read DARKER (44) than the floor it stands on (53), so every other height cue was arguing against the most basic one and losing; each room boundary was drawn as two independent blocks because adjacent rooms each author their own wall, putting a lit/dark seam down the middle of one stone mass; the camera clamp had silently cancelled the wall-height frame extension since the day it shipped, so a room's tallest wall never showed its top; the per-wall stone-relief filter cost 10-32 render targets per room for a measured 0.06% difference and is now off; and both the sphere shading and every ground shadow were sized against a rig's DECLARED body radius while the shipped art paints only 0.68-1.00 of it — which had been painting a hard-edged dark disc onto the background around every crystal enemy, recorded in an earlier session as an over-large shadow. Fixed with an additive cap key light, run merging, a per-room floor light pool (the cheap static half of design/01's parked lightmap), and art-derived body radii guarded by a test that re-decodes the real PNGs every run. One wall then took four more reports on 2026-08-19, all of them the same defect: every tonal constant had been measured on an EAST-WEST wall, where the cap is a thin band under a lit coping, and applied unchanged to a north-south run, where the cap is 100% of what you see. The flat additive key light was destroying the swatch's contrast RATIO (2:1 to 1.4:1) and is now the cap drawn a second time in `add` mode; caps tile in world space so an L corner's two blocks share one stone field; the east band no longer paints a grey stripe down a run's whole top; and a deep run now TUCKS under the wall it meets, stopping just below that wall's crown course — the longest unbroken horizontal in a room, and the line the eye identifies a back wall by. The crown line is measured per element, because `wallface_ice.png`'s coping band is a third shorter than fire's — a bug found by the new `client/src/game/scene/wallComposition.test.ts`, which runs the real level-1 floors through the real wall pipeline and decodes every shipped face swatch to check the constant against its art. **The art-direction blanks are closed as of 2026-08-25**, all three of them locked design rules that had shipped half-built rather than optional extras: design/13's element colour-plus-icon pair is a *dual-channel* rule and only the colour channel had ever existed, so the five icon glyphs (flame/snowflake/bolt/skull/gem) now ride on elemental mobs, on each active status aura, in the HUD's damage badge and on a dropped weapon — vectors, not art, and drawn as solid masses because a stroke is invisible at the 3-8 px these render at; the poison biome got its three swatches, so every element in the closed five has real stone (its brief carries a gameplay clause the others do not — a green floor makes poison bullets and poison-tinted mobs camouflage, so "dial the green down" is stated numerically in the prompt and asserted on the shipped pixels); and weapon rarity's overlay spec is locked as **count, not hue** — four of the five rarity hues turned out to already sit on a reserved element hue and `common` white is byte-identical to physical's neutral, so no palette could have separated them, whereas a countable 0-4 marks cannot be confused with a hue at all
- [x] Client performance analysis (`client/src/perf/`, 2026-08-24, ported from the sibling project `funny`): a windowed frame-timing monitor that runs in EVERY session and names which half of the frame (update vs render) a sustained stutter came from, plus `?perf=1` for an on-screen readout and a WebGL draw-call/program-switch probe. It found this client's actual bottleneck the day it landed — a per-actor lighting filter costing one render-target pass per character (render p50 10.4ms -> 2.4ms once lighting became one screen-space pass). A same-day follow-up added `drawAttribution.ts`, which attributes draw calls to actual objects rather than counting them, and used it to take the frame from 165 draw calls to 108: the wall cap's additive key light is now pre-multiplied into its swatch (a blend-mode change breaks Pixi's sprite batch on both sides), and the structurally-static layers get their own render groups so an actor's per-frame `zIndex` write stops discarding the floor's and the shadows' cached draw instructions. Both verified pixel-identical by GL frame read-back (0 of 1,641,600 pixels). Forcing the same batching inside the Y-sorted `entities` layer was measured and REJECTED (+0.7ms for -50 draw calls) — see `client/src/render/staticGraphics.ts` for the rule that came out of it, and `client/src/perf/README.md` for the before/after numbers, the deviations from funny's original, and what is worth attacking next.
- [x] Accounts (Phase 6, `design/16-accounts.md`): real username/password login (SQLite via `node:sqlite`), never required to play — bound to PvP ladder rating and Forge blueprints/materials/loadout; third-party OAuth reserved, not built
- [x] Internationalization (Phase 7, `design/17-i18n.md`): English-canonical `t()` system with compile-time key checking, 8 locales (中文/Deutsch/Français/Español/Polski/Русский/Italiano), every screen migrated, a language-cycle Settings control, first-boot browser-language auto-detection — enum/data-driven values (damage type, weapon kind, rarity) deliberately left untranslated
- [x] Screen-flow completion (design/10 follow-up, 2026-08-03): a boot loading screen; a menu-driven Mode Select (solo PvE / co-op / PvP solo queue / tutorial) replacing PLAY's direct jump into Forge — co-op/PvP were previously boot-flag-only; a real Matchmaking screen (connecting/error/cancel/retry), fixing a latent bug where a dropped connection hung forever with no feedback; a standalone, always-skippable tutorial level (move/aim/fire → weapon-swap → melee-deflect → the extract-vs-descend checkpoint decision)
- [x] Level 1 balance pass (2026-08-17, `ENGINE_VERSION` 41): a headless PvE level simulator (`npm run test:pve-sim`) — bot-driven real runs reporting per-room reaction window / peak simultaneous shooters / clear rate, with difficulty gates — and the rebalance it measured out: a per-room concurrent-fire budget, staggered room wake-up, halved garrisons, renewable shield regen, plus a door-lock softlock it caught. See `design/05` "Room encounter budget"
- [x] Room feel pass (2026-08-17, `ENGINE_VERSION` 42): the camera frames the current room instead of the whole floor and centres the character rather than their feet; bullets are drawn leaving the gun's actual barrel tip (the sim's ground-plane muzzle and the rig's screen-space one sit on parallel lines — corrected on the view, so hit detection stays authoritative); enemies collide with each other again, gained a perception radius inside the existing room-activation gate, and move slower; the shield shimmer breathes instead of strobing. See `design/05` "Room feel pass". Level 1 is measurably easier as a result — re-tightening garrisons is tracked as open work there
- [x] Wall clearance (2026-08-19, `ENGINE_VERSION` 43): the character stops at its own body radius against a wall or a pillar instead of at its 7 px feet circle — from a play report that walking into a corner read as sinking into the stone. New `Actor.solidRadius`, split off `footprintRadius`, which keeps its old value and now covers only actor↔actor push-out (a body overlapping another body reads as a crowd; a body overlapping stone reads as sunk into it). Enemies keep their old clearance, so no mob path or level-1 garrison measurement moves. See `design/07` and `design/ROADMAP.md` "Sunk into the wall"
- [x] Occlusion x-ray (2026-08-20, client-only): a standing wall block or pillar that is currently drawing over the player goes translucent, from a play report that the character *"跑到墙下面去了"* — walked to the north side of an interior block and vanished (measured: the body's rect read luma 78.4 against the cap stone's 77.1, i.e. arithmetically indistinguishable from the wall). No single layer was wrong — a block's art intrudes one wall HEIGHT north of its own footprint onto walkable floor and sorts in front of it, the player's clearance puts them at most 16 px into that band, so a 70 px block covers all 32 px of the drawn body and per-object sorting offers no "partly hidden". The default pass fades the CAP only (fading the whole block was tried on the live frame and reads as a hole in the room), so the face, the silhouette and the ground shadow keep the mass solid; a pillar has no cap/face split and fades its whole shaft, since that is what a character behind it disappears into. A second pass takes the face too where a tall wall on a shallow footprint buries the body below the cap/face fold and a cap fade would achieve nothing — 1.3% of the floor. Then *"加测试"* produced `occlusionCoverage.test.ts`, which sweeps all 97,803 positions the player can legally stand at across the five shipped floors against an independent overlap oracle, and immediately falsified two claims this session had already written into design/01: a perimeter run DOES trigger the x-ray (a wall between two vertically stacked rooms was the *lower* room's 104 px north perimeter, not the upper room's 22 px kerb — so 72 px of that room's floor made the player completely invisible; fixed at the tier the same day, see "Wall tier" below), and a wall's face DOES cover the player on a shallow footprint. Measured over level 1: 5.5% of standable floor made the character 100% invisible before, none of it leaves more than half hidden after (worst case 43.8%). See `design/01-rendering.md` "The occlusion x-ray" and `design/ROADMAP.md`
- [x] Door grid alignment (2026-08-20, `ENGINE_VERSION` 44): a door's passage rect now lands on a whole grid cell, from a geometry audit that the occlusion sweep above prompted — four wall runs in the shipped level-1 content stood **16 px deep** where every other wall on all five floors is a full 32. Not an art bug and not a merge artifact: `DOOR_EDGE_MARGIN_GRID` is 1.5 and the anchor step is `span / 4`, so a drawn passage could land on a half or even a quarter cell, and `carveDoorGaps` — doing exactly the right rect-difference — then cut a correspondingly misaligned hole, leaving whatever remained of the wall run past it fractionally deep. A 16 px footprint under a 104 px-tall perimeter run is the worst case for the standing-wall art (a cap band on a third of the depth every wall tone was measured on) and it is the geometry that made the x-ray need its second, face-fading pass at all. Fixed in three places because any one alone leaves the hole open: the drawn anchor is snapped in a new shared `world/dungeon/doorAnchor.ts` (the copy of that math each of the two placement functions carried verbatim is now the one thing they share, so a fix cannot land on one path and miss the other — the procedural path was the worse of the two, with 33 fractional passages across a 10-seed sweep); level 1's nine fractional `passageGrid` values are floored to whole cells as data, which is what actually removes the four runs from a real run; and the map editor's save gate rejects a hand-typed fractional passage, which it never did even while holding `solids` to whole cells. `DOOR_EDGE_MARGIN_GRID` itself stays 1.5 — it is the fit threshold, and no integer value satisfies both halves of what the tests already pin, which the suite now demonstrates rather than asserts (setting it to 1 fails 5 tests, to 2 fails 6). See `design/05` "Door position is authored freely" and `design/ROADMAP.md` "The 16 px wall runs"
- [x] Wall tier: the boundary between stacked rooms (2026-08-20, client-only): `wallGeometry.wallTier` decided a wall's height from the one room the wall's **centre** falls in, so "am I a south boundary?" could only ever be asked about that one room — and where two rooms stack vertically the boundary between them is *two* walls, one grid row apart, authored by two different rooms. The upper room's own south wall kerbed correctly at 22 px; the lower room's north wall answered "I am my room's north edge" and stood at the full 104 px, one row south of the exact floor the kerb tier exists to keep clear. A block's art rises from its own north edge, so it reached a measured 72 px into the room above — 22 runs of it, on all five shipped floors. This is the *"kerb rule is defeated for any shared boundary"* follow-up the occlusion sweep found and the entry above recorded; the x-ray was keeping the player visible there, which is work a correct tier does not need done. The rule now asks *"does any room's floor lie immediately north of me"*, one predicate covering both halves of a shared boundary, which strictly generalizes the old test (a room's own south wall is the case where the room to the north is the wall's own room). No splitting pass is needed and that is the point: every room authors its own four perimeter walls, so a boundary arrives as two independently-tiered rects and `RoomBuilder` tiers **before** it merges — a collinear north boundary shared by two side-by-side rooms, only one of which has a room above it, therefore keeps its tall half, where the old rule had merged the two into one 32-cell perimeter run. Measured over level 1 with the fix stashed and unstashed: the share of standable floor that leaves the character at least half hidden before the x-ray drops 8.5% → **5.4%**, completely invisible 5.5% → **3.3%**, the deep face-fading pass 1.2% → **0.2%**, and 19 runs move from perimeter to kerb (105 → 86 perimeter, 37 → 53 kerb). Confirmed on a live frame too (luma scan down world x=350 at floor 0's `r4_forge`/`r5_extraction` boundary: stone used to start at y 440 = `544 − 104`, now starts at 492 = `512 − 22`). PvP was untouched at the time — the then-current 60-room arena's rooms all carried `solids: []`, so it had no wall runs at all. **No longer true as of 2026-08-25:** the launch map `arena_launch` walls every room (492 runs, merging to 294 drawn blocks), so the arena exercises the wall renderer for the first time; its walls are confirmed to build and draw without errors, but nothing has yet judged how they LOOK there. All five of the wall/door/x-ray CONTENT SWEEPS were extended to it 2026-08-26 (`client/src/game/scene/arenaWallCoverage.test.ts`, 28 tests) — the composition rules all hold on it, and four things do not: an arena builds no door fixtures at all (so the door-clip rules were dead there and 36 of its 74 passages were buried under a wall's cap — fixed later the same day, below), the x-ray fades four pillars at one spot where the PvE sweep bounds it at two, the map hides the player three times as often (11.6% of standable floor fully invisible vs 3.3%), and the worst block's shading geometry is 208 floats against level 1's 120. **Both of those last two were then answered on 2026-08-26.** How it LOOKS: the buried doorways are the one real defect and they do not read as one (the covering run is continuous, plausible stone, so a player never tries to walk through) — which makes them a READABILITY defect rather than a blemish, and they were fixed the same day by feeding `arenaMap.doors` to the clip rule (58 covered passages → 10, 0 buried, plus a worn floor patch at all 74 thresholds; fully-hidden floor 11.6% → 7.8% as a side effect of 44 runs getting shorter). The four-pillar fade and the hiding fraction both read correctly, because whatever fades keeps its outline. What it COSTS: the 208-float wall shading was the wrong suspect — 294 wall blocks cost 2 draw calls, while 124 PILLARS cost 245 of 278, each carrying a 496-float base-crease `Graphics` over Pixi's 400-float auto-batch line. Converted to a sampled ramp the same day: **262 draws / 254 program switches → 14 / 6**, unbatched `Graphics` in the scene 124 → 0. **A frame-TIME number was finally obtained the same day, and it inverted this entry's own premise.** It did not need a device — it needed real Chrome instead of the in-app browser pane (the pane is a software rasterizer with no timer-query extension; the same machine's Chrome is an Intel Arc Pro that has it). `arena_launch` costs **~4 ms of GPU time per frame** at 1920x855 (36 draws / 20 programs), with both controls firing: an empty target reads 0.000 ms and a 0.5/1.0/2.0 resolution sweep moves 3.20/4.31/5.93 ms with non-overlapping bands. That sweep is the finding — 16x the pixels for 1.85x the time, i.e. **~3.0 ms fixed against ~0.7 ms of fill** — so the frame is not fill-bound and the quality tiers aim at a fifth of it. Attribution against a 4.05 ms frame: `ground` **2.28 ms (56%)**, `shadow` 0.63 ms, and `entities` — all 294 wall blocks and 124 pillars — **0.39 ms (10%)**. So "no culling, 492 runs resident" was aimed at a tenth of the cost: `buildGroundLayer` paints wash/mottle/decals **per room** into two batch-forced `Graphics` that are **284,966 and 265,566 floats** on this map, against the ~50k and ~24k `staticGraphics.ts` states its policy was measured on. Not fixed (it carries a design choice), but now GATED offline by `client/src/game/scene/groundGeometryBudget.test.ts` — the aggregate was headlessly measurable all along and had no reader, so the drift was invisible to 3,284 client tests. See `design/01-rendering.md` "Every wall stands, at one of three heights" and `design/ROADMAP.md` "The wall between two rooms was standing on the wrong floor"
- [x] The occlusion x-ray generalized to every enemy, and a door could still be blocked by a wall (2026-08-20, client-only): the x-ray above only ever built its focus from the local player, so a monster standing in the exact hidden band a wall's art intrudes over — live report *"如果只有怪物在墙下面的话，就看不到怪物了"* — got no x-ray at all and rendered fully swallowed; `occlusion.updateOcclusion` now takes a list of foci (local player + every live enemy), and a block fades if it hides ANY of them. Separately, a door standing immediately north of a wall run has no x-ray to fall back on at all — it renders on `layers.ground`, strictly behind the Y-sorted wall it would need to fade against — so a deep north-south run spilling its cap past its own footprint (the same "art intrudes one wall height north of its footprint" mechanism as the x-ray case) could visibly cover a door (*"门不能被高墙挡住了。门应该是随时清晰可见的"*). Fixed at the geometry instead: `wallRuns.bordersDoorNorth` finds a run whose north edge meets a door passage, and `blockCapTop` clips that run's cap to stop at its own footprint. That clip alone only ever helped a DEEP run, and a sweep of the real shipped pipeline (`doorSpillCoverage.test.ts`) found the SHALLOW shape — the more common one, since an ordinary wall's thickness is almost always shallower than its tier height — still firing 12 times across all five floors: a block's FACE is drawn at a fixed tier height regardless of its own footprint depth, so for a shallow footprint the face alone reached past the run's edge with nothing left for a cap-only clip to touch (measured: 72 px of pure face on a 32 px-deep stub). `wallRuns.effectiveWallHeight` closes it by shrinking the height fed to both the face and the cap. **Neither clip ran in PvP until 2026-08-26:** `GameState.dungeonDoors` is populated in dungeon mode only, so an arena handed `RoomBuilder` an empty door list — 58 of `arena_launch`'s 74 authored passages had wall art over them and 36 were covered to their full depth, which is the same defect this entry describes fixing, in the mode that never fed the fix its doors. `RoomBuilder.build` now unions `s.arenaMap.doors` into that list (the rule needed no change to work on arena content: 44 runs match, 21 of them the shallow case, residual 10 partly covered, worst 40 px, none buried) and the same list drives the worn floor patch at all 74 thresholds. Door FIXTURES stay dungeon-only — an arena `Door` has no lock and no leaf, and its passages are meant to stay open — so `doorFlankTier` is still unexercised there. Two method notes came with it. The offline verification of this fix had been measuring a HARNESS that replicates `RoomBuilder.build` rather than calling it: it reported every one of the fix's numbers correctly while the shipping code did nothing, and landing the fix left all 951 scene tests green — so the harness now applies the clip itself and one test drives the real `build()` and reads its occluders back. And the first mutation battery (11 hand-picked mutants, all killed) was redone as a coverage MEASUREMENT over the whole call chain (41 mutants, whole client suite as oracle, failing test files recorded per mutant): **3 survived, all of them the worn patch's `WEAR_ALPHA`/`WEAR_COLOR`** — `drawDoorWear`'s geometry was covered and its VALUE by nothing, so an invisible patch passed 3,208 tests. Closed by asserting the additive luma it contributes (faintest band > 3, centre < 45; measured 5.62 / 22.49) against every floor tone, with the three mutants run through the same arithmetic as the bounds' own control. 41 killed, 0 survivors. A follow-up pass also removed `LIT_WALLS`, a per-segment stone-relief filter kept switched off since 2026-08-19 (measured at a 0.06% mean frame difference) rather than deleted — dead weight with no re-tune ever queued up, gone outright rather than left as a permanently-false switch. See `design/01-rendering.md` "The occlusion x-ray" and "A north-south run is not an east-west wall", and `design/ROADMAP.md`'s entries for the same date.
- [x] Doors stand up, and the floor stops at its rooms (2026-08-20, client-only): the scene passes so far had gone into the character and the walls, so a floor-wide `renderer.extract` was measured to find what else was wrong — and the biggest thing was that a door was still a flat `Sprite` on `layers.ground`, stretched to its passage rect, while every wall around it stood 22-104 px. `door_{locked,open}_raw.png` are front ELEVATIONS (a portrait stone frame around a hazard slab; the same frame as an empty arch), i.e. the exact mistake `design/13` records for the wall swatches before 2026-08-18, still live for the one fixture a player has to read at a glance — 221x320 of portrait art squeezed into 64x128, and into a 128x64 LANDSCAPE rect for a north-south passage. `scene/doorRender.ts` now builds a door as a wall block whose FACE is an opening: because a passage rect is a hole in a wall, the mass above a doorway lands exactly where a wall block's cap lands and the opening lands where its face goes, so one construction covers both orientations, from the same shell `wallRender.ts` uses (its cap/face/edge builders are now shared, not re-implemented). A door stands as tall as the wall it interrupts and never taller (`wallRuns.doorFlankTier`) — 11 of the 24 doors on the shipped floors are cut into a 22 px kerb, which is low precisely so it cannot stand between the camera and the player, and a doorway there gets its read from an additive hazard bloom instead of from height. The leaf is fit by width and cropped, never squashed (fitting both axes compresses the kerb case 8:1), and both PNGs were re-trimmed through `tools/png-pipeline/compress.mjs` — they carried ~33 px of transparent margin per side, differently on each state, so the leaf covered 67% of its own opening and the two states were not registered against one another. Doors are also x-ray occluders now, which they could not be while they lived on `ground`. **The floor was the second half of the same pass.** It was one `TilingSprite` of a 256 px swatch over the world, with two separate defects: three 48x48 samples 512 px apart in three DIFFERENT rooms came back byte-identical (an exact 8-cell period, the same floor everywhere), and — the bigger one, which the first measurement had not thought to ask about — 29-56% of it was painted where no room exists at all, because `worldW/worldH` is the bounding box of a floor's co-resident rooms and a `graph2d` layout's rooms do not fill their own bounding box (1.41-2.26x their area across the five shipped floors; on floor 0 the surplus is a single featureless 1500x430 field with no walls and no room light in it, which reads as one enormous room the level happens to have wall-lines drawn across). The floor now stops at the rooms, and `floorCoverage.test.ts` proves that is safe by flood-filling every shipped floor from every room centre through non-wall cells and requiring each reachable cell to be inside a room rect — while the same sweep used to be why a PvP arena kept the whole-world floor (5240 of `arena_prototype_60`'s 11,524 non-wall cells were reachable and outside every room rect and passage, so a per-room floor there would have put a player on the backdrop). That premise died with that map: as of 2026-08-26 `groundLayer.floorRegionsPx` MEASURES it per map instead of branching on map kind, and `arena_launch`'s floor stops at its 60 rooms. On top of that: a per-room warm/cool wash (room floor means now span 40.6-54.2 luma), mottle at a scale incommensurate with the tile grid (64 px patch-mean sd 2.69 → 4.23), a mirrored per-tile stamp instead of one tiling sprite (mirrors only — a seamless tile stays seamless mirrored and does not rotated), and wear: stains, rubble lit from the same upper-left key light as everything else, and a worn patch across each doorway along its travel axis. Two first attempts were wrong in ways only a 2x crop showed (a hashed per-tile tint painted the tile grid as a checkerboard of flat rectangles; mottle bands at a flat alpha drew visible arcs). `RoomBuilder.ts` crossed 500 lines doing this, so the ground stage moved to `groundLayer.ts` (539 → 424 lines, no behaviour change). A follow-up coverage audit (*"有测试可以加吗"*) widened the pass's mutation battery from 12 mutants to a scripted 21 over every branch and constant in the new code, and **16 of them survived** — the tests written alongside the code, measurements in their comments and all, covered about a quarter of the decisions it makes. All 16 are now dead (1941 tests, +65 for the pass), including the ones that mattered most: the recess and the sill removed entirely, a cropped floor tile ignoring its source offset (a duplicated strip along every room's east and south edge), the mottle's additive half dropped, the grid mounted under the floor, and the per-room seed replaced by an array index. Two needed a second attempt at the TEST rather than the code — an assembly assertion of the form "the fixture contains this geometry" passes trivially when the geometry is empty, and a "two rooms differ" assertion has to compare shapes relative to each room's origin and filtered to the shapes themselves, since Pixi emits its own `moveTo(0, 0)` before each one. The one item this pass measured and did NOT fix — pillars reading as smooth cans beside the stone walls — was closed the same day; see the next entry. See `design/01-rendering.md` "A door is a wall block whose face is an opening" + "The floor stops at its rooms", and `design/ROADMAP.md` "Doors that stand, and a floor that stops at its rooms"
- [x] Pillars get real art (2026-08-20, client-only): the last item on the scene queue, and the one the previous pass measured but left — a pillar's cap was a hand-toned gradient where a wall cap is a real swatch under an additive key light, so four smooth cylinders stood in a room of textured stone. `pillarRender.ts` had already recorded, in 2026-08-18, why the obvious fix does not work: sampling the 256 px WALL swatches through a ~35 px cap window lands on one arbitrary dark patch, and the brick elevation on the shaft reads as an open-topped well. A fixed-size object (`radius: 1` in every shipped room, drawn 84x98) needs art authored at its own scale, so this ships as one whole-object SPRITE — and **one file for every biome, not one per element**: `pillarTint()` multiplies the biome's wall colour over it, the same channel the hand-toned body already used. Seven generations, six rejected, and the rejects carried the lessons: **five were generated at exactly 84x98**, the size the game draws — but `FxController.MAX_ZOOM` is 4.5 and the renderer runs at up to 2x device pixel ratio, and level 1's gallery room actually renders at zoom 4, so a sprite is MAGNIFIED in real play and art at game size is simply a blurred pillar (the one defect that cannot be repaired at import, hence an explicit output resolution in the prompt and a 384 long axis where every swatch in that directory uses 256); one read as an open-topped barrel, exactly the drift the prompt's own negative constraint names; one had a top surface at luma 125, the brightest thing in the room, repeating the defect the 2026-08-19 measured pass fixed; and the real near-miss — a per-element fire attempt that followed the canvas/margin/aspect spec exactly but drew its course joints STRAIGHT (centre-vs-edge sag −2 px against the accepted file's +53) and put 21% of its height into the top surface against the accepted file's 30%, i.e. two files that would ship as two camera angles in a game with one fixed camera, for a difference of 1.8% warm pixels that a tint gets for free. The accepted generation then needed two import steps, neither visible by looking at it: it came back with a transparency **CHECKERBOARD painted in as opaque pixels** (3.46M opaque, zero transparent — the standing "decode the alpha, don't trust the picture" rule in a new costume, since the file did not lack alpha so much as depict it), keyed out by luma and flood-filled from the border to prove no interior holes; and its SHAFT measured twice the value of the wall face beside it (lit limb 71.6 against 27.3) while its top was already on target at 87.3 against wall caps of 72-81 — same stone, same key light, reading as a different material. A uniform multiply cannot fix that, since darkening the shaft drags the on-target top down with it, so new `tools/png-pipeline/lumaCurve.mjs` scales RGB by a gain chosen from each pixel's OWN luma: top untouched, shaft bands 84/59/30 → 57/40/20, which is the same fold between a bright top and a much darker face that `wallTone.ts` builds for the walls, applied once offline instead of as a per-object filter. Measured live after (extract of the real gallery room at zoom 1, sample rects derived from the sprite's own bounds): top 87.3, lit limb 50.4, mid 35.8, dark limb 16.7, foot 25.0, against wall caps 72-81, wall faces 27.3-27.5 and a floor of 48.5 in the same frame. Two would-be silent bugs avoided on the way: the occluder box the x-ray reads is measured off the SPRITE (`pillarArtExtent(bodyW, height, tex)`), never off the hand-toned ellipse maths, and the sprite key loads with `autoGenerateMipmaps: true` and WITHOUT the `addressMode: 'repeat'` every tiling swatch gets — a 326 px source drawn at 84 px is the same 4:1 minification that turned the rig art into colour noise in 2026-08-12, and `repeat` on a lone object samples the far side of its own silhouette. Then *"有测试可以加吗"* twice over: a scripted 21-mutant battery (18 over the code, 3 over the shipped ASSET — revert the curve, apply it uniformly, compress to game size) came back with **3 survivors**, and a second 18-mutant battery over the decisions the first left alone came back with **11**, in three clusters worth naming — the contact crease's GEOMETRY was invisible to tests that only read fill alphas (it could be mounted under the sprite it shades, sized off the art's height instead of the room's, or lose its corner rounding, all green); the biome could be dropped silently now that one file serves every element (`getPillarTexture('neutral')` hardcoded, or the sprite tinted from the neutral palette); and three `lumaCurve` mutants survived tests written for exactly those properties because the FIXTURE made the mutation equivalent — a bright transparent pixel is skipped by the gain short-circuit anyway, a hard step still comes out monotonic, and two pixels whose red channel and luma fall on the same side of the band cannot tell a luma-keyed gain from a channel-keyed one. All 14 closed; both batteries now kill everything (21/21 and 18/18). 1968 client tests + 30 in `png-pipeline` (the tool had none — `applyLumaCurve` was extracted from its CLI to be testable, and two tests drive the real CLI, because the entry-point gate that decides whether the pipeline step does anything at all is itself a decision). New `pillarArt.test.ts` decodes the SHIPPED PNG and measures it — real alpha with transparent corners, resolution headroom, the aspect the `WALL_HEIGHT` agreement depends on, a top that is the brightest plane, a shaft in the wall face's family, no baked base darkening — because the pillar's whole look now lives in a binary file, where no test of the code can see it. See `design/01-rendering.md` "A pillar is a sprite now", `art/biome/prompts.md` "Pillar SPRITE", and `design/ROADMAP.md` "Pillars get real art"
- [ ] WeChat mini-game adaptation (see `design/04-wechat.md`) — the adapter, the real-art loading path and the 4 MB package budget are done, gated in CI, and confirmed in the real simulator; **2026-08-25: five WeChat-only bugs found and fixed, each one hiding the next** — `Game`'s constructor threw (`URLSearchParams` missing on that runtime, not the previously-suspected shader) so boot never reached `Game.start()`, every menu `Button`/`Slider` was silently unclickable (Pixi's interaction system never receives anything on a canvas with no real DOM event API — the in-run twin-stick controls were unaffected, since they bypass Pixi's interaction system entirely), and then, once tapping worked, the Forge was a dead end because every menu screen is laid out for a viewport about twice as tall as a landscape phone's 844x390 (fixed by a fit-scale on the menu layer, `client/src/game/ui/menuLayer.ts`, swept over every screen at five viewports), then entering any room threw because `capLight` built its baked wall-cap texture with `Texture.from(canvas)`, which identifies a canvas by `instanceof` against DOM globals this runtime lacks, and finally every label in the game rendered blank because Pixi sets `context.letterSpacing = '0px'` before every measurement and every `fillText` and that one assignment poisons a wx 2D context (`measureText` goes non-finite, `fillText` paints nothing — bisected in the running mini-game at 1058 painted pixels without the step and 0 with it). The last two are the same class as each other: Pixi identifying a browser by touching a DOM global, on a runtime that has some of them and not others — and note that the DevTools simulator DOES provide `document`, which is what let browser-only code look healthy there. **Also 2026-08-25: the shaders were confirmed to run there (27.4% of the frame is the lighting pass, measured with two liveness controls firing), and the render-quality tiers that a slow device needs now exist** — every perf number in `design/01` had been measured on a desktop Chrome, and until this change a device that could not afford the frame had nothing to turn off. What remains: real-device verification (frame rate, touch feel, lowest base-library version), for which `design/04-wechat.md` now carries a step-by-step **On-device test plan**
- [x] Room props get real art, and three loaders that were never mip-mapped (2026-08-24, client-only): the last Graphics placeholder in a level-1 room, taken on because the user asked to **validate the level's art and the client's performance before more feature work** rather than discover a shortfall after. Three sprites from four generations (`prop_{crate,barrel,rubble}.png`), and the reject is the interesting one: the first rubble came back as pale diagonal splinters, median luma 61 against the ember floor's 34, reading as bone chips on dark stone — backwards from design/13's "environment desaturated, hazards saturated", where a light heap reads as loot. Its TONE was fixable offline with `lumaCurve.mjs`; its silhouette was not, which is what decided the reroll, and the reroll worked by naming the primitive with a count ("BLOCKS, not shards"), stating the value target numerically (median ~45, highlights <=100 — it landed 48/95) and naming what a wrong value would be mistaken for. **A new alpha defect class came with them**: all three decoded as textbook bimodal alpha and were not, carrying a veil of alpha 1-10 reaching 50-140 px past the object with the body at 252-253 rather than 255. Both ends are invisible (4% and 99% opacity) and `alpha-audit.mjs` reads the pair as one "suspicious" file with no opaque pixels at all — but `trimAlphaBoundingBox` keeps any pixel with `alpha !== 0`, so the veil became part of the object: the rubble trimmed to aspect 2.95 against its real 3.67, and since a prop is scaled by WIDTH with the art's aspect setting its height it would have stood 25% too tall, above 123 empty rows that a bottom-anchored sprite turns into clearance off the floor. New `tools/png-pipeline/alphaClamp.mjs` snaps both plateaus and runs BEFORE `compress.mjs`; every file shipped before this measures identically at `alpha > 0` and `alpha > 25`, so the `alpha !== 0` trim had simply never been handed a file where it mattered. Landing the art also exposed a live integration gap — `buildPropBody` took a `BiomePalette` and, on the sprite branch, never read it, so `PROP_BIOME_MIX` stopped applying to anything the player could see the moment real art arrived (new `propTint`, the same mix-into-white trick `pillarTint` uses; it deliberately does NOT re-hue the crate, whose warm lean is wood and whose separation from the loot crate is VALUE, 53 against 167, not hue). Content went from 15 placements across 8 of 14 pieces to 54 across all 14, swept by `propContent.test.ts` against solids, both spawn kinds, the door band and each other — a validator written BEFORE the content, which rejected nine of the first placements. Then two findings that were not about props at all: verifying the props' mip chains meant dumping every texture in a live frame, and **`weaponSkins.ts` and `uiSkins.ts` were still passing a bare url to `Assets.load`** — a mounted weapon measures 5.3:1 minification in a real room (320 px source, 60 px on screen), worse than the 4:1 that made the pillar need a mip chain, on the object every actor in the frame carries; and design/12's last facing gap (a character facing away still showing its front belly chamber) closed with **no new art** via a `FRONT_ONLY_BONES` set gated on there being no `__back` texture, so a future `belly__back` PNG supersedes it for free. Tooling: `perf/frameProbe.ts` packages the A/B/C frame diff every art pass had been retyping, with the guard this one paid for — an hour went into diffs that all read zero because the scene under test was never on screen (`Game.beginRun()` is callable from the console but does not hide the main menu, which draws over everything in `layers.ui`), and **the restore check cannot catch that**, since hiding something invisible changes nothing and C equals A. `probeFrames` therefore requires a liveness control, runs it first, and reports `trustworthy: false` when it moves zero pixels. Client performance is **not** validated and the reason is recorded rather than papered over: this machine's Chrome reports `Microsoft Basic Render Driver` — a software rasterizer with no `EXT_disjoint_timer_query_webgl2` — so real GPU time is unmeasurable here. **(Corrected 2026-08-26: that describes the IN-APP BROWSER PANE, not this machine. Real Chrome on the same box is an Intel Arc Pro on D3D11 with the extension present, reachable via claude-in-chrome, and GPU time has since been measured there — see the arena entry above and `client/src/perf/gpuTimer.ts`. Generalising this sentence from a surface to a machine is what left the PvP arena with no frame time for a whole step.)** One result survived two runs (removing the `fx` layer's `BlurFilter({strength:3, quality:2})`, 4 of the frame's ~7 full-screen passes, roughly halves the cost at 390x844 DPR 3) and the rest of the attribution did not, since removing ALL filters measured slower than removing one, which is physically impossible. A 加测试 pass then ran a 38-source-mutant plus 12-content-mutant battery: **27/38** first run, and the nine real gaps were all in the newest code, in two recognisable shapes — every `frameProbe` fixture was the same kind of degenerate (all grey, so swapped Rec.601 coefficients agreed; all non-negative coordinates, so the lower clamp was never walked; all ascending scan order, so the percentile sort was invisible), and the DEFAULT path had no test because every test passed an override (`defaultControl`'s undo could have been a no-op, permanently destroying the scene it measures; the `probe` handle forwarded its caller's callbacks and replacing them with no-ops survived the whole suite). `readFrame`/`frameRectOf` had zero coverage on the grounds that this workspace has no DOM, which `vi.stubGlobal('document', ...)` disposed of. Content mutants killed 12/12 first run; second source run 37/38, the survivor being a genuinely equivalent mutant recorded as such rather than chased. 4220 tests. See `design/ROADMAP.md` "Room props get real art", `art/props/prompts.md`, and `client/src/perf/README.md`
- [x] The shield becomes a shell, and a shader you can finally measure (2026-08-25, client-only): a user report with a red circle drawn round the hero — *"现在的护盾是一个圆圈包裹着角色，我希望的是类似一个透明的蛋壳一样的效果将角色全部包裹，而不是一个圆环"*. The 2026-08-24 fix above had un-squashed this shield back into a true circle, which fixed the ellipse and left the OTHER half of 圆圈 standing: the shader still drew `smoothstep(a, b, dist) * (1.0 - smoothstep(b, c, dist))`, a band with a hole in it, so a shielded character stood in empty space with a hoop round its waist — a ring is not a small shell, it is a different object. `EnergyShieldFilter` now paints a glass sphere: a filled interior (the one thing a ring cannot do — every previous version computed exactly zero at the centre), a Fresnel limb off a sphere normal (`pow(1 - nz, 3)`; linear washes the whole disc into a flat cyan blob, which is what reads as a decal), one specular glint at ~0.6 of the radius, and a radius grown 1.55 → 1.87 body radii because 全部包裹 includes the mounted weapon, whose barrels had been sticking out through the shell. Two of those numbers came from LOOKING at a real frame rather than at the source: the glint at 0.45 of the radius landed on the body, where an additive highlight over near-white character art is invisible, and the interior fill had to be damped by the body's own alpha (`FILL * (1.0 - 0.55 * color.a)`) because undamped it flattened the hero's face — the saturated blue eye came out the same pale cyan as the shell around it. The shell still stops short of the feet, so the ground shadow the 2026-08-19 volume pass added reads through its ~8% interior alpha. Then *"可以加测试吗"*, and the answer needed a different KIND of test: the existing shield suite reads the shipped GLSL as text, which is the wrong evidence for a question about a shape, since two shaders can satisfy every regex while painting completely different things. There is no GL context under vitest on this machine and a TS reimplementation of the equation (the approach `sceneLightModel.test.ts` takes, correct for a lighting formula) cannot catch a STRUCTURAL regression — it would go on computing a shell while the shader drew a ring. So `shieldShellModel.test.ts` interprets the real thing: a GLSL-subset evaluator (tokenizer, Pratt parser, GLSL broadcasting, the builtins this shader uses) executes the shipped `main`, prelude functions and `frameUv`'s pow2 remap included, duplicating no constant and no formula, and throwing rather than skipping on anything it cannot evaluate. 25 tests over the shape (centre painted, no hole out to the surface, circular at 16 angles, gone past the fade, a glint off-centre and inside the shell), the transparency, the fade with the shield pool and the breathing pulse, and the region-centring across four region/pow2-texture mismatches — the crescent bug that took this filter three separate fixes is a CASE now, not a comment — plus three tests on the evaluator itself, since one that quietly mis-evaluates makes every measurement agree with anything. Running it found two things reading the source had not: the composite is not monotonic (the shimmer bands radially, so a ray outward ripples ~0.03% of peak — pinned as a magnitude, because a ring dips 100%), and an alpha bound read at `uTime = 0` is a lucky sample that the `glow * 0.7` composite knob mutated to 1.0 walked straight through. 16 mutants, **16 killed by the measured suite alone**, and the split with the source-contract suite is informative rather than redundant: the regex suite misses "`uIntensity` ignored" entirely, and catches a dropped `frameUv` with one failing test where the measured suite catches it with fifteen. See `design/01-rendering.md` "The shield is the deliberate exception" and `design/ROADMAP.md` "The shield becomes a shell"
- [x] A character carries a gun AND a blade, in both modes (2026-08-26, `ENGINE_VERSION` 45): from a play report with a red circle drawn round an empty patch of HUD — *"角色可以同时持有一把近战武器和一把枪，并且在ui上我标注的位置可以进行切换。我记得之前实现过一次"*. Every layer of weapon swapping had shipped and was individually green — the `SWAP_WEAPON` button, `ApplyInputSystem.swap`, the keyboard/touch paths, and the HUD's idle-slot chip at exactly the circled spot. What had gone missing was the SECOND WEAPON: `if (seat.loadout)` treated `[]` as "the player chose this" (empty arrays are truthy), so a fresh save — and every run after the previous one consumed its crafted weapons — fell back to a lone auto pistol, and the chip hides itself below two weapons. `resolveLoadout` now owns one rule for the whole game: unknown ids dropped, the staged weapons kept active-first, and every free slot filled from `PLAYER_BASE.startWeapons` with a kind the loadout does not already cover. PvP got the same invariant by its own route — the `landing_basic` kit is a scaled blaster + saber, kept as arena-scoped authored content so the fairness wall stays structural — which also means parry is reachable from the drop there, so `design/03`'s ranged-vs-melee trade-off moved from a build choice to a moment-to-moment one, and `design/15`'s TTK tuning inherits that. Coverage was chosen by MEASUREMENT rather than guesswork: a mutation battery over `resolveLoadout` (14 mutants + controls) reported two survivors that turned out to be EQUIVALENT — the fix was to delete a dead early return, not to write a test — and the shipped bug replayed verbatim left the pre-existing HUD suite fully green, which is what proved the new end-to-end cases (`client/src/game/gameWeaponSwap.test.ts`, HUD tap → sim) were closing a real hole rather than restating one.
- [x] The shell gets thickness, and the character finally goes inside it (2026-08-26, client-only): started as a question about Legend of Mir 2's 20-year-old 魔法盾 and ended as *"没有被蛋壳包裹的感觉 … 边缘的那个圈太过实线了"*. Two structural findings, neither a tuning problem. **`pow(1 - nz, 3)` is a ring whatever the surrounding code calls it** — it equals 1 only AT the silhouette, so the 2026-08-25 shell above was a flat `FILL` plate with all its energy in a hairline; measured on a real frame, its profile peaked at `b = 1.00` and held above half-peak across 0.10 of the radius, which is what 太过实线 is numerically. It is now the chord of the view ray through a shell with an inner and an outer radius (`THICKNESS` 0.22, a number the user set by dragging a slider in a live WebGL A/B before any code was written): peak at `b = 0.80`, half-peak width 0.30 — and the GLSL interpreter predicts 0.78/0.31 while a luma cut off the GPU frame says 0.80/0.30, two independent measurements of the same claim. **"包裹" is a compositing order, not a shape** — every version added the whole shell ON TOP of the character, so nothing was ever behind it; scaling the back hemisphere by `(1.0 - color.a)` puts the body between the two halves, and that one multiplication is worth more than every silhouette change. Plus refraction by the sphere normal (the filter already samples the character's texture, so it is nearly free), an elastic dent on impact whose envelope is `exp` decay times `cos` so the surface rebounds past rest rather than fading — the shield had had no hit reaction at all, `ActorFilters.hitFlash` drove only the white outline — and a seamless irregular-scale membrane tile **generated at boot rather than authored**: its two hard requirements are seamless tiling and a known grey range, exactly what an image model is worst at and a 40-line Voronoi is exact at, and it costs zero bytes against `design/04`'s 4 MB main package. Perf was measured, and **the first harness was blind**: `performance.now()` around a render loop reported the new shader as cheaper than the old one, and the control — a 200-iteration shader, ~1000x the ALU — came back at 2.35x, which is impossible. With `EXT_disjoint_timer_query_webgl2` (same control: 10.8x), over a 768 px region on an Intel Arc Pro: old 0.162 ms, new **0.223 ms (+38%)**, new with the radial cull disabled 0.321 ms — so one `if (b > CULL) return;` skipping the ~58% of the square the shell never reaches pays for most of the rewrite. Then *"有测试可以加吗"*, and the honest answer was that the 41-mutant battery's "0 survivors" said nothing about the WIRING, because it never mutated `EventReactor.ts` or `actorFilters.ts` — where the only coverage was one `expect(hitFlash).toHaveBeenCalled()` and every `Actor.test.ts` call site passed no arguments. Extended to **53 mutants over five files, 0 survivors, 5 controls surviving**, and it found: refraction that never faded with the pool (so a draining shield left the character warped and then un-warped in one frame when the filter detached, landing on the break burst), 35 tile cells whose independent random constants collide into 34 distinct values at 8 bits (~90% likely, so two scales always died together — now a shuffled rank), a limb-fade term that could be computed and never used because the assertion read the term instead of its effect, and a control that died for a good reason: `HIT_FLASH_MS` 160 → 165 broke a pre-existing test *named* after that constant which drove its decay from a magic `80`. See `design/01-rendering.md` and `design/ROADMAP.md` "The shell gets thickness"
- [x] The arena gets a frame time, and it was not the walls (2026-08-26, client-only): the last thing blocking Step 4's closeout was that `arena_launch` had **no frame-time number at all** — only GL counters — and design/04's on-device list carried "needs a handset" as its best-justified item. It needed a browser surface, not hardware: the in-app pane is `Microsoft Basic Render Driver` with no `EXT_disjoint_timer_query_webgl2`, which an earlier session correctly recorded and which then got generalised from *surface* to *machine*; real Chrome on the same box is an `Intel Arc Pro` that supports it. **~3.8-4.3 ms of GPU time per frame** at 1920x855, 36 draws / 20 programs / 10 framebuffer binds, ticker stopped and renders hand-driven. Both controls fired — empty target **0.000 ms**, and a 0.5/1.0/2.0 resolution sweep at **3.20 / 4.31 / 5.93 ms** with non-overlapping bands — and that sweep is the result, not just the control: **16x the pixels for 1.85x the time**, so ~3.0 ms is fixed and ~0.7 ms is fill. The frame is **not fill-bound**, which means the render-quality tiers (whose whole mechanism is removing full-viewport passes) are aimed at about a fifth of this scene. Attribution by hiding one render-group root at a time, against 4.05 ms: `ground` **2.28 ms (56%)**, `shadow` 0.63 ms (16%), `entities` — every wall block and pillar — **0.39 ms (10%)**. `ground` measured FLAT across the whole 16x pixel range, so it is submission, not fill: `buildGroundLayer` paints wash/mottle/decals **per room**, and 60 room rects make those two batch-forced contexts **284,966 and 265,566 floats** against the ~50k / ~24k `staticGraphics.ts` records its policy as measured on. Left unfixed on purpose (per-district split vs dropping `batchMode` at arena scale is a design call, and the measurement now exists to judge either), but **gated**: `scene/groundGeometryBudget.test.ts` sweeps `ARENA_CATALOG` against a budget anchored to that ~50k, exempts `arena_launch` by explicit list, **asserts the exemption still FAILS** so the known defect is the gate's own control, and pins the shipped aggregates exactly off the real `RoomBuilder.build` — which reproduces the live browser `census()` byte-for-byte, so this was offline-measurable the whole time and simply had no reader. Judged mutant-by-mutant against the suite WITHOUT it, **7 of 16 defects it catches were caught by nothing else**, including `drawFloorMottle` deleted outright. Two things the batteries corrected in my own work: a 2% tolerance let `drawRoomWash` deletion through at **0.18%** (pins are exact now), and a "floats per room" band was only valid at the room size it was measured at (5.3k at 512px, 55.5k at 2048px, because the passes count from AREA). Tooling: `client/src/perf/gpuTimer.ts` packages the recipe with its controls (`sweepTrust`, `resolutionSplit`), for the same reason `frameProbe.ts` exists. Four ways a GPU-timing run lies, all hit here and all recorded: a background tab throttles rAF so `ticker.FPS` read **60.2** while the sampler banked 333 ms in six seconds; `setTimeout` is clamped to ~1 s there so a result poll must not yield; a fully-backgrounded tab eventually sets `GPU_DISJOINT_EXT` on **25 of 25** samples (the harness reporting `null` is correct — foreground the window, do not relax the check); and a blank frame reads as a fast frame (one reload measured 1.63 ms because that run's arena never came up, caught by a luma control at 48.6 vs 15.8). **Still open:** the on-device run, which this sharpens rather than replaces — the dominant cost is geometry submission, the half that gets relatively worse on a mobile GPU. See `design/01-rendering.md` "The arena's frame, measured on a GPU", `client/src/perf/README.md`'s fourth measurement, and `design/ROADMAP.md` "The arena finally has a frame time"
- [x] The shell gets an exit, and three guards that guarded nothing (2026-08-26, client-only): the one item the pass above left open — `shield_break` dropped `EnergyShieldFilter` from the composed list on the frame the pool hit 0, so the shell vanished between two frames and `EventReactor`'s burst carried the whole moment. It now plays a ~200 ms EXIT (`uShatter` 0→1) while `ActorFilters` holds the filter ATTACHED, the same "keep the view alive past the state change" shape `startDissolve` already uses: the outer surface throws itself open, the wall thins to a rim so the bright band migrates out to the silhouette (half-peak width 0.141 → 0.077 of the radius, read off a real GPU frame), each membrane scale is thrown outward at its OWN speed off the tile's existing extinction-rank channel, and the light collapses to *exactly* nothing — the shader hands back the untouched source texel at `uShatter` = 1, which is what makes the detach that follows invisible. The fragments went to `ParticleSystem.shieldShards` rather than into the shader, for a reason the open note did not have: `Actor` pins that filter to a fixed square, so a shard the shader drew would simply stop existing at ~2.4 body radii. The predicted fight with the radial cull never happened either — putting the expansion into `surface` rather than into `b` means the cull radius, measured in surfaces, grows with the shell for free; what actually bounds it is the filter's own area (0.44 · 1.30 · 1.18 = 0.675 against a hard 0.707), and the suite scans the real visible edge at every instant instead of trusting that. **The battery's real finding was three guards that read correctly and guarded nothing** — a no-double-start check unreachable behind `setShield`'s own ratio short-circuit, an overshoot clamp whose value is written and reset in the same call so no frame can render it, and a "no shell to break" check that changed nothing because nothing re-attached the filter at that moment. The fix was not to test them: the state model was over-guarded, and rebuilding it around two independent facts (`shieldActive` = the pool is up, `shatterMs >= 0` = the exit is playing) left ONE reachable guard carrying both properties. One attempted fix was itself scaffolding — an `apply()` added to make the last guard observable turned out to be dead code in the correct program, proven by a follow-up mutant and reverted. **51 mutants over five files, 0 survivors, 6/6 controls surviving**, plus a LINEAR ramp that had survived an ease-out assertion on floating-point noise and a `shatter` setter nothing tested because every measurement drove the uniform through the harness. The GLSL evaluator was EXTENDED rather than routed around (a positional tile sampler, since a per-cell throw displaces the lookup and a constant texel makes any displacement measure identically), which bought the assertion the section exists for: hand back a tile bright at the displaced fetch and dark at the probe, and ask which one came out. Perf with `EXT_disjoint_timer_query_webgl2` (control at 13.3×, two runs agreeing to three decimals): **+9.4% at rest** — two extra tile taps that at `uShatter` 0 land on the texel the probe already pulled in — peaking at +45% for the 200 ms of the exit, which is area rather than instructions. **That measurement failed twice first**, both times plausibly: a run that rotated the variant order reported the bigger shader as 3.5× *cheaper*, because the page's own game loop was drawing into the same GPU between queries. The control is what said so. Then *"有测试可以加吗"* again, and the answer was that **"0 survivors" is always scoped to the files a battery mutates** — that one had mutated the shader, the tile and the three wiring files, but not the CALL CHAIN that connects any of it to the game. Mutating `Scene.ts`, `Actor.ts`, `GameLoop.ts`, `FxController.ts` and `engine/config.ts` instead produced **seven survivors against all 3239 tests**, two of them the same defect the arena sweeps kept finding — a mechanism that is correct, unit-tested and connected to nothing: `v.setShield(Math.max(1, p.shield), p.maxShield)` in `Scene.reconcile` (**the shell can never break**, so the entire exit animation is dead in the shipped game) and `scene.interpolate(alpha, 0)` in the main loop (every per-actor clock frozen — the shell exit, the hit-flash decay, the death dissolve, the burn haze — nothing animates, ever). Plus the pool ratio invertible, the pool hard-codable, the enemy shield sync deletable, `SHIELD_REGEN_DELAY` 90 → 3 (regen cutting the 200 ms exit short in real play), and `particles.update` never advancing so every burst spawns and hangs in mid-air. All seven killed by four tests, and the two lessons are the general ones: **source one case from the real PRODUCER** — the new `Scene.test.ts` case drives the engine's own `takeDamage` until the pool empties and asserts BOTH halves of the moment off that single call (`shield_break` in the events, `shield === 0` reaching the shell through `reconcile`), which is the only way to check the wiring's claim that the two need no handshake; and **a survivor can be a CONTENT gap** — the enemy sync survived because every shipped enemy has `maxShield: 0`, so the fixture supplies the pool the roster does not have yet rather than the line being deleted as dead. The regen coupling is asserted as a MARGIN (`SHIELD_REGEN_DELAY` ms > 3 × `SHATTER_MS`), so either constant can be retuned and only a real collision fails. See `design/01-rendering.md` and `design/ROADMAP.md` "The shell gets an exit"
- [x] The game gets a sound library, and none of it plays yet (2026-08-27, assets only): **556 CC0 files audited, 46 shipped** (`client/public/audio/`, 95.0 kB of mono MP3) covering **15 of the 16 `AudioCue`s** — `status.burn` keeps its synth voice because no fire crackle exists in any of the six Kenney packs. **Nothing loads them**: `platform/audioSynth.ts` still generates every cue live, so the game sounds exactly as it did before; the blocker is the cue catalogue `design/11` still lists as undesigned. The audit found what a free pack hides — 43 files clipping to **+3.51 dBFS**, 233 with leading silence that would land as latency on a deflect, 247 bit-identical dual-mono — and the format choice was measured rather than assumed: OGG/Vorbis carries a ~3.6 kB codebook header per file, so for clips this short **MP3 is 2.6× smaller** and decodes sample-exact where Vorbis adds up to 30 ms of padding (OGG stays right for music loops, where that header amortises). Nobody has **listened** to the 46 files — they were picked by spectral fit against the synth voice each replaces plus the world's own material (`design/13`'s crystal-mirror enemies, so glass); measurement rules out defects, not taste. Licence texts and a sha256 per pack are archived under `art/audio/`, the pipeline and its 15-case self-test under `tools/audio-pipeline/`, and the invariants tying files ↔ `credits.json` ↔ the `AudioCue` union are gated by `client/src/platform/audioAssets.test.ts` (12 tests; all 9 injected mutations caught). Music and ambience remain absent, and CC0 music turned out to be almost entirely chiptune — a style mismatch for the flat-cel direction.
- [x] The floor was made cullable, and then made to stop at its own walls (2026-08-27, client-only): the arena's dominant render cost, closed in two steps. `layers.ground` was **56% of a ~4 ms GPU frame**, and the first step made it cullable — one piece per room, each tagged with the rect it paints, switched off per camera — which cut the batcher's packed vertex data **17x and moved the frame by nothing measurable**, a null result that was the finding: the cost was never geometry submission. The second step found where it actually was. A mottle blob reaches **460 world px past the room that seeded it** (~1970 screen px at the shipped zoom of 4.29), so standing in one room legitimately put four dark and four light halves on screen with the three neighbours painting 1.31 and 1.91 extra viewports of pure spill — **45% of the floor was rooms the camera is not in**. `scene/floorClip.ts` clips each blob to its own room as it is built, so the cull rect became the room and the existing cull dropped the neighbours on its own: **0.53-0.93 ms off a ~4.4 ms frame** across three counterbalanced GPU sessions, on-screen pieces 13 → 7, 19.9% fewer floats. The load-bearing part is not the millisecond but **where a cut on a floor is allowed to land**: a hard clip at the room rect leaves a 29.98 luma step across a doorway, so the clip ramps its five bands across the one grid cell of stone that every shipped room rect turns out to contain (measured over six maps: 100% wall-or-passage at 2/16/30 px inside every room edge, bare floor at 34), which takes the worst step to 2.59 against a bound derived from the mottle's own profile. In a live frame the clipped doorway ends up **smoother** than the unclipped one (worst per-pixel step 36.16 → 18.51), because the roughest thing there had been a neighbour room's rubble speck — which the clip drops whole rather than cutting. Four mutation batteries across the two steps (29 + 20 mutants over the cull, 26 + 17 over the clip), and a sweep widened from one map to six; the widening caught a bound that had passed only because it was measured on `arena_launch`. Along the way: three frame readers and three GPU-timing runs that each returned a confident wrong answer, all recorded in `client/src/perf/README.md`'s sixth and seventh measurements. **Everything above is a luma measurement, and on 2026-08-27 the pass was finally LOOKED at** — the clip is invisible standing in a doorway, invisible across two district seams, and invisible across all 60 rooms in a single whole-map frame, and the five places the arena's own content sweeps had named as "point a camera here" all came back acceptable (the 2x2 colonnade reads as four columns going hazy rather than a hole; the deep fade is the one verdict with a reservation, reading as a glass pane). It found one new thing, an authored-content question rather than a render one: the map's **twelve deliberately-empty grid cells read as hard-edged black rectangles with no rim**, ~20% of a 16:9 frame from the room beside them, wherever the void is east or west of the player rather than south of it — only a void to the south is seen across a wall run's top surface and dark face. See `design/ROADMAP.md` "The camera list, answered", which also carries the harness lesson that cost the most: a backgrounded Chrome tab gets FROZEN, which leaves synchronous JS working perfectly while every `await` hangs and `drawImage(app.canvas)` silently returns the last presented frame. **That last finding is closed by the entry below**, which also corrects the "~20% of a 16:9 frame" figure: a large part of that rectangle was the scene-light pass darkening the rooms beyond the empty slot, not the void.
- [x] The void gets a face (2026-08-27, client-only): the one finding the camera list left open — `arena_launch`'s twelve deliberately-empty grid cells reading as a hard-edged black rectangle with no rim, no depth cue and no far side — and the scope turned out to be neither twelve cells nor an authoring pass. It is the **projection**: `screen.y = gy - z` has no horizontal component, so a block's east and west sides are exactly zero px wide and its art simply stops at the footprint's edge. Harmless where a neighbour's floor or stone carries on; a cliff where the next thing along is the void. A void to the SOUTH never reads that way because a whole wall height of lit FACE stands between the floor and it — which is why one direction was reported and the other was not. Same rule at the map's outer silhouette, and on every PvE floor: **83 free sides on the arena, 108 across the five floors**, against a "twelve cells" framing. `scene/wallVoidEdge.ts` answers where a wall ends at nothing (in SPANS, not booleans — the arena's east-west runs meet the empty slots END-ON, 32 px of a 64 px side, which is the "端头" the report actually named), and `scene/wallVoidReturn.ts` carries the cap's own swatch 16 px past the footprint **in the same world tiling**, so the mortar runs straight on over the arris rather than restarting — tinted for a vertical surface (the two tints are bracketed against `CAP_TINT` and `FACE_TINT`, i.e. argued from the key light rather than eyeballed) and ramped out to the backdrop with a new squared profile (`render/shadeRamp.powerRamp`; a linear fade across 16 px is already half gone at its midpoint and reads as a bright line with a smudge beside it). Measured on the live frame: the boundary went from 53 → 26 → **6** in three pixels to 53 → 26 → **83** (a lit arris) → 44 → 2 over 30. NORTH is deliberately out and that was **checked, not reasoned** — a block's cap already reaches a full wall height north with a coping, and `cameraFrame` overscans by exactly `MAX_WALL_HEIGHT`, so the camera cannot see past it (verified at the widest room with an empty slot above it: viewport top world y 1414 against an art top of 1400). The pass also found that shipped content sits **exactly** on a limit nobody had noticed: two walls facing each other across a void each reach inward, and `ember_l1` floor 2's narrowest void is 32 px — precisely twice the reach, zero margin — so each span now carries its measured gap and the renderer clamps to half of it, making the property structural instead of contingent. Two mutation batteries (38 + 6 mutants, 2 controls): the first's nine survivors included **two tests that had been written and had never landed** (a scripted edit whose anchor silently missed) and **two that indicted the CALL SITE rather than the rule** — dropping `RoomBuilder`'s argument, and feeding the predicate room rects instead of the painted floor, both survived the whole 3,400-test client suite. The second round's one survivor is the sharpest lesson: "all 83 returns share one ramp bake" passed on shipped content while still allowing a per-block cache key, because every shipped reach happens to be identical. See `design/01-rendering.md` "The void gets a face" and `design/ROADMAP.md`.
- [x] The pane was never the fade, it was the extent (2026-08-27, client-only): the last of the five camera verdicts still carrying a reservation — the occlusion x-ray's **deep pass** (the fallback that drops a block's front FACE as well as its cap) *"reads as a glass block with hard edges, since a ghosted rectangle is more pane than x-rayed stone."* Both obvious readings of that are wrong: it is not the 0.34 fade and not the full-strength silhouette round it, it is the **extent**. `needsDeepFade` decides WHETHER a face has to go translucent and nothing decided HOW MUCH OF IT, so the whole face went — and on the shipped deep case (70 px of art over a 32 px footprint) a body at the closest legal approach occupies the face's **top 22 px** while the other 48 px is drawn over the floor BETWEEN the character and the wall. Dropping that took the block's dark base course, its plinth and its footing on the floor and gave nothing back; three arms at identical framing (shipped deep fade / cap-only / fully solid, camera pointed at the worst sample `arenaWallCoverage.test.ts` already knew) bracket the answer — the deep pass has to reach past the cap/face fold, and only as far as a body. `occlusion.deepFadeReach(height, footprintDepth)` bounds it as geometry rather than taste: a focus stands NORTH of the footprint and a body is drawn UPWARD from its ground point, so the lowest face row any body can reach is `height - footprintDepth` below the fold, full stop. `wallRender.addWallFace` draws the face as two pieces on that row — the band keeps `XRAY_DEEP_LABEL`, the base takes `FACE_BASE_LABEL` and is in **neither** x-ray group, so it holds full strength through both fades like the silhouette does. **32 of 70 px (46%) of every deep block on the map stays opaque**, the split changes pixels at rest **nowhere** (splitting all 227 splittable blocks in frame moved only the one faded block's base rows — 3.49% of the frame, all inside a single 513x91 box), and it costs **no draw call** (45 → 45; the extra `TilingSprite` batches). The new hard join mid-face was measured rather than eyeballed: **12.99 luma** across the seam against the **22.73** the same wall already carries at its own cap/face fold when solid, and the split's worst per-row step anywhere (13.62) lands 9 px BELOW the seam on a stone course inside the now-opaque base — so no feather, which would have cost a stack of graduated sub-pieces since a `FadeGroup` can only scale alpha multiplicatively. Two things about the rule's shape are load-bearing: it is derived at clearance **ZERO**, because `foci` includes every live enemy and an enemy keeps its FEET circle against solids (`enemies.ts`, as low as 6 px) so a mob legitimately stands 10 px closer than the player-only sweep can place; and a DOOR is excluded by passing no reach at all, since its passage floor is INSIDE its own footprint and a character in the doorway stands on exactly the rows the derivation excludes. The invariant that makes the split lossless — every focus the deep pass fires for is inside the band — is asserted on all 778 deep-firing samples of the arena's 72,686 and on the five PvE floors, not on the rects the rule was derived from. **Five battery rounds, 79 mutant runs over 51 distinct mutants, 2 controls intact; 8 survivors — 7 now tests, 1 an equivalent mutant** whose survival is the evidence for the drive-by in the same pass (Pixi's `Container.destroy` calls `removeFromParent()` itself, so `RoomBuilder`'s `portal?.shadow?.destroy()` — the last copy of a redundancy removed twice before — could go). Round 1's survivors were all about the code; **every survivor after it was about which content the tests run on**, and the second round found one thing that was not a gap but an ERROR: both 70k-sample coverage oracles still modelled the deep pass as leaving *nothing* opaque, i.e. they credited the x-ray with visibility the renderer no longer delivers. Correcting them was not enough either, and that had to be measured rather than assumed — with the reach cut by 24 px the corrected oracle reports 25% of the body buried and the acceptance assertion it feeds (bounded at HALF the body, the right bound for the x-ray as a whole) still passes. What the split guarantees is exact, so both sweeps now assert exactly that: zero rows of the body behind a deep-faded block, per (sample, block) pair, which makes a reach cut by ONE px fail. Also closed: the split sat outside every draw-call budget (a base drawn as a `Graphics` fill would look identical and cost a draw call on 227 blocks — the same shape as the void return's Graphics one pass earlier), the base holding alpha through a real fade was covered only by accident via a filter named `silhouette`, and a tautological `expect(base.label).toBe(FACE_BASE_LABEL)`. The survivor that took three rounds is the most useful: feeding the occluder box the TIER height where the block is drawn at `effectiveWallHeight` survived everything, because every fixture in `RoomBuilder.test.ts` is a room WITHOUT A DOOR and the two heights differ only for the 12 shipped shallow runs beside one — a missing fixture, not a weak assertion, and the third time in a week a call-site mutant turned out to be immortal on the only content its file had. See `design/01-rendering.md` "The deep pass stops where the body does" and `design/ROADMAP.md` "The pane was never the fade, it was the extent"
- [x] The membrane was multiplying nothing, and a knife replaced the gun (2026-08-27, client + engine, `ENGINE_VERSION` 46): two reports, and the shield half was three defects wearing one symptom. *"护盾中间的6边形看不清，看起来还是一个圈"* — (a) the tile was never hexagons, just a jittered Voronoi over a squashed metric with a test named *"scales, not a honeycomb"*; it is a triangular lattice now, whose Voronoi diagram **is** a hex tiling, so the generator below it is unchanged and only the points moved. (b) The pattern MULTIPLIED the shell, and the shell's interior is deliberately ~0.11 because it composites over the ground shadow — so there was nothing there to scale: measured on a rendered frame at gameplay zoom, **9 of 255** across the whole middle, reaching ~30 only in a thin annulus at `b ~ 0.8`. A pattern in one ring with nothing inside it is a circle. It ADDS now, off a **zero-mean signed** tile encoding whose mean is subtracted by measurement rather than tuned, so it costs nothing on the brightness budget the multiplicative form was protecting — mean luma over the shell disc is 83.0 with the membrane and 83.2 without, checked on the live frame. (c) *"之前调试效果时看起来挺好的，和游戏里实际表现差别有点大"* is the reusable one: every term this shader has is additive, and the middle of a shielded actor is the hero's near-white body, over which the shell's own green and blue are already past 255 — an additive membrane there is not dim, it is arithmetically ABSENT. The debug view had a dark background where the game has a bright character. The pattern now also rides a MIX toward the shield colour, which has no ceiling. Then *"改成椭圆或许更好，高度上长一点"* and *"整体缩小一点，类似紧贴着角色…缝隙和图里枪的直径差不多"*: the shell is 1.30 taller than wide (nothing in the GLSL says "ellipse" — the shader is isotropic in region-normalized uv, so the shape is entirely `Actor`'s `filterArea` aspect) and pulled from 1.87 to **1.53 body radii**, clearing the body by one gun-diameter. That gun had to be MEASURED: the weapon sprite is 24 x 16.35 world px but is mostly transparent margin, and its opaque box is 15.75 x 8.55 — the sprite rect would have set the clearance at 1.02 body radii and left the shell where it already was, the same trap as the `BODY_FILL` mismatch. Net region area ends 13% SMALLER than before either change. Separately, `PickupSystem.applyWeapon` overwrote whichever slot was ACTIVE, which destroyed the one-gun-and-one-melee invariant v45 had just been bumped to establish — the starter pair is `[blaster, saber]` with slot 0 active, so the first melee weapon off the floor left you carrying two of them, no gun and no route back; it matches on `w.spec.kind` now, the same test `resolveLoadout` fills slots with. Two shield tests were PASSING on a stale literal (`SHELL_R * 6 / sqrt(2)`, the old region copied in as a `6`), reporting 1.87 body radii for a 1.53 shell. 8 new tests, 6 mutants, 6 killed. See `design/01-rendering.md` and `design/ROADMAP.md` "The membrane was multiplying nothing"
- [x] The sound library gets a catalogue, and 46 files stop being decoration (2026-08-27/28, client-only): the previous pass shipped **46 CC0 mp3s that nothing loaded** — `audioSynth.ts` still generated every cue, so the game sounded exactly as it had before. This is the wiring, and the thing that was missing was **data**: a cue catalogue. `client/src/audio/` is now `cueCatalogue.ts` (an exhaustive `Record<AudioCue, CueDef>` — variant count, gain, voice-cap priority — whose paths are **derived** from the cue id rather than listed, because a typo in a list of 46 filenames fails *silently*: a missing file is indistinguishable from "no sample, use the synth"), `SampleBank.ts` (best-effort fetch+decode through a new `readBinary` on the SAME asset-host seam the art loaders use, so audio inherits the WeChat bundle rules and a future music subpackage is a prefix rule, not code), `decodeAudio.ts` (one promise over the browser’s promise-form and WeChat’s callback-form `decodeAudioData` — assuming either shape would leave one target silently loading nothing, forever), `VoiceBudget.ts` (12 voices, priority-ranked, stolen with a 12 ms fade, retired by **time** rather than an `ended` event: a cap that stopped purging would fail *closed* and look like "audio broke"), and `CueMixer.ts` (sample-if-loaded else synth, with the catalogue gain applied to **both** rungs so the fallback is not louder than the thing it stands in for). `EventReactor` now coalesces to `Map<cue, count>` instead of a `Set`, so ten hits in a frame become one impact **at higher gain** rather than ten. Verified in a real browser rather than assumed: 46/46 files fetched 200 and decoded, a 3-second firing burst produced **27 sample voices and 0 synth voices** (9 further cues dropped by the cap, all `muzzle` — the ladder working), and peak PCM sampled on the SFX bus reads 0.000 silent, 0.067 for a `deflect` sample, 0.021 for the `status.burn` synth keep, and 0.092 → 0.136 (**×1.49**) for one vs ten coalesced impacts. **82 new/changed tests in 7 files**, two of them integration tests that exist because the units cannot fail for the thing that actually goes wrong — audio breaking *silently*: one drives real `GameEvent`s through the real reactor/backend/mixer and asserts a frame of all 16 cues plays **15 decoded voices and exactly one synthesised**, the other runs the real WeChat preload against a `wx` fake backed by the real mp3s with the browser globals deleted and `decodeAudioData` in its callback-only form. **35/35 injected mutations caught** — three of them earned their keep: a real crash (a cap of 0 indexed into an empty voice list), a **fake-fidelity bug** (the `wx` fake ignored `readFileSync`'s encoding argument, so "read the mp3s as utf8" — which on a device hands a string to the decoder and loses every sample — passed the suite), and a guard that turned out to be observable only through its message. Writing the saturation test also corrected my own account of the design: over the cap a cue is not refused, it **steals** the weakest slot. Two things this does NOT do: **nobody has listened** to the 46 files — they were picked by spectral fit, and now that they actually play that is the top open item on `design/11` — and **music is still absent**, whose selection problem is nothing like the SFX one (CC0 music is overwhelmingly chiptune, a direct mismatch for the flat-cel direction).
- [x] The void gets a far side (2026-08-28, client + docs): the rim shipped the day before answered *where does the stone end* and left the other half open, filed as an art call with three candidates — **a pit, open sky, or ground beyond the wall**. The projection settles it, and **a pit is unbuildable here for the reason the original bug existed**: what sells a pit is the far bank's INNER wall descending from the rim, and across an east/west void that wall faces east or west, which `screen.y = gy - z` gives exactly zero projected width — the one surface that would carry the depth is the one this projection cannot draw. A pit FLOOR does draw, being horizontal, but `z < 0` pushes it DOWN the screen, so a pit `d` px deep lands `d` px SOUTH of its own footprint while the Y-sort puts it BEHIND the room down there; visible depth is bounded by the empty screen space south of the void, which for an interior cell surrounded by rooms is about zero. Sky works in the projection but inverts the contrast the new rim depends on (its east arris is drawn LIT precisely because the backdrop is luma ~6) and splits at the two scopes the rim unified, since past the map's outer boundary `wallVoidEdge` reports an `Infinity` gap and there is no horizon to key on. **Ground** is a horizontal plane at z = 0: no zero-width surface in it, the backdrop stays dark so arris, tints and squared ramp all stand unchanged, and it is one rule at both scopes. `scene/Terrain.ts` is **two display objects and two draw calls, fixed** — a `TilingSprite` of generated noise and a `Sprite` of `Texture.WHITE` tinted `palette.void` over it — and it computes **no void geometry at all**, because the floor paints over the plane wherever there is floor and the walls wherever there is stone, so what is left showing IS the void. `layers.terrain` is a child of `world` but a SIBLING of `lit`: the frame that closed the camera list had already found that much of what read as “the void” was the scene-light pass darkening the rooms beyond, so putting the plane inside `lit` would hand it back to the pass that was blacking it out. It is fitted to the camera's visible world rect each frame **above** the `activeQuality().sceneLight` early return, same position and reason as the ground cull, since the low tier is the device tier. The swatch is **generated, not a PNG** (`shadeRamp.bakedField`, a `BufferImageSource` — no `DOMAdapter`, no 2D context, no canvas-free fallback to maintain), which matters against WeChat's 3.41 MB of a 4.00 MB main package. Measured on a live frame, the void beside `r5_extraction`: **before mean 27.70, sd 0, min = max** — not one pixel of that rectangle differed from any other, so “flat” was literal rather than rhetorical — against **after mean 30.31, sd 1.41, range 23.3–33.5**, with the lit floor in the same frame at 47.66. Floor-over-void 1.57x, and the floor stays 13x more textured, which is what reads as distance; the boundary scan runs cap 76-93 → **arris 88** → trough 6 → terrain 24 → 28, against the rim's own row that fell to 2 and stayed there. **Two defects, each findable by exactly one method.** The frame caught a legible tile repeat (a 64 px two-octave swatch whose dominant octave's period IS the tile, so the eye locked onto a grid of identical blobs; fixed with a 128 px tile and a third octave) — every test was green before and after. A 76-mutant battery (**45/76, then 58/76 after six more tests**) caught the biome derivation: `terrain` was tinted from the neutral terrain like every other biome colour, but mixing the same absolute amount of a bright element hex into a darker base lifts it more in relative terms, so neutral's terrain/ground sat at 0.75 and ember's at 0.85 — and ember is the only biome that ships. The ASSERTION was wrong too, which is the reusable half: **any ratio of terrain's luma to ground's drifts toward 1 as the hue brightens**, so the bound that survives a hue is positional, not proportional. The battery's three real gaps are each a named class: **seamless is not continuous** (the tile-wrap test compared column 0 with column TILE and said nothing about the middle — three mutants kept the wrap and put a hard step at every cell boundary, the same visible-grid defect the frame had already caught once; shipped field 0.108 max adjacent step against the mutants' 0.52/0.62/0.88); **a clamp no shipped input reaches is still live at an EXPORTED entry point** (`Math.min(255, ...)` is dead at every palette colour and reachable through `terrainSwatch(anyColour)`, where a `Uint8Array` wraps 309 to 53 rather than saturating — and fixing it half-way was its own lesson, the re-run killing the RED channel's clamp while green and blue survived because the new test read only `buf[i]`); and **three self-referential constant assertions** (`expect(fog.alpha).toBeCloseTo(TERRAIN_FOG_ALPHA)` is a tautology, the contrast test computed its bounds from the constant it was checking, and `TERRAIN_MIX <= 0.5` was a boundary that let the constant walk to exactly 0.5 with everything green). Two further gaps came from reading rather than the battery, whose operators cannot generate them: the fog must paint OVER the plane (reversed, the haze does nothing and every other assertion still passes), and `fitTerrain` must receive the camera's world rect rather than the viewport (identical at zoom 1, wrong at every other zoom — and the first version of that test ran at zoom exactly 1, which its own control caught). The 18 remaining survivors are recorded rather than chased and judged by measuring: nine are the hash's internals, producing a **different but equally valid field** (same range, mean, continuity and seam behaviour — nothing specifies the exact noise values). Also in this pass: design/04's on-device plan still told a person holding a phone that a `render`-side breach meant the floor's batched geometry needed a fix design/01 describes, carrying the FOURTH measurement's inference that the fifth had already falsified — the cull cut submission 17x and moved the GPU frame **not at all**. That prediction is retired; the plan now carries the `[perf]` `update`-vs-`render` reading with none attached, which is what would make a device breach informative. See `design/01-rendering.md` “The void's far side” and `design/ROADMAP.md` “The void gets a far side”.
- [x] The gun never moved, and the flash was aimed at the floor (2026-08-30, client + engine): one report — *"角色射击时，没有射击动画，枪口也没有射击特效，看起来非常死板，而且子弹出现的也很突兀"* — and all three symptoms were **existing features wired to the wrong thing**, not missing ones. The muzzle flames and casings had fired on `bullet_fired` since 2026-07-26, anchored on the event's own position: the SIM's muzzle, a flat `muzzleOffset` along the aim ray on the **ground plane**, plus a hardcoded 12 px lift. That is not where the rig draws the gun, and the gap is not subtle — sampling the correction `Scene` already applies to the bullet VIEW on real shots in a live room gives **12.4 / 14.2 / 14.3 world px**, most of a body radius, ~35 screen px at the room camera's measured 2.67x. So the fx burst beside the character's middle and the report is literally correct: there was no muzzle effect, because there was nothing at the muzzle. `bullet_fired` gained an `ownerId` (fx-only — the event queue never enters `serializeState`/`hashState`, so **no `ENGINE_VERSION` bump**) and every muzzle fx now anchors on `Actor.muzzlePos()`, the same drawn barrel tip the bullet has spawned from since 2026-08-17. The radial `flash()` was replaced by a directional `muzzleFlare` that **collapses** over 85 ms instead of expanding over 170 — at 170 ms a 200 ms-cooldown weapon has one on screen permanently, which reads as a glowing barrel rather than as shots. **The "no shooting animation" half has a finding in it**: an `attack` clip is authored in all three `char_*` bundles and has never been played, and it stays unplayed *(superseded 2026-09-02 — the entry below supplies the additive layer this one says would be needed; the reasoning here is still why that layer is additive)*. Clips here are sampled WHOLE, and the four enemy bundles ship no `attack` at all — so playing it would do nothing for any mob, and for a hero would drop every bone the clip does not track back to rest for its duration (orb-core's `attack` touches only `socket_r`, so `idle`'s hover bob would snap to 0 on every shot and back 350 ms later; at a 200 ms cooldown held fire would pin the body at bob 0 and release would pop it). It also kicks in RIG space, not along the aim, and the mount code never read its translation, so the gun would not have moved even where it did play. `render/rigRecoil.ts` is a 150 ms envelope layered OVER whatever clip is playing; because the offset is applied to the MOUNT rather than to the sprite, `muzzleLocal` recoils with it for free — barrel tip, bullet spawn correction and muzzle fx all follow the gun. One path, all seven rigs, no authoring change. Bullets also stopped being drawn identically on the frame they were born (an oversize core settling to exactly 1, with a departure flare, inside 90 ms). **Verified live** through `window.__game` against a real run — the pane cannot present frames, so pixel readback is not evidence here (hiding the whole world layer and re-rendering returned a byte-identical canvas); the render TREE is what was measured: real enemy `bullet_fired` events resolve to live actor views 4/4, and one shot at aim 0.893 rad moves the module -6.25 authoring px at the peak (`cos(0.893) x 10` = 6.27), the drawn muzzle -3.25 world px and the body -1.87, all back to 0 by 150 ms with the render clock as the only input. **+78 tests, and the battery is the interesting part**: 18 mutants, 16 killed, **2 survivors, both real holes**. `muzzleLocal` dropping the body's own displacement survived because the test asserted a DIRECTION and the 10 px module kick swamps the 3 px lean; a spawn pop shortened to 0.0001 ms survived because the test that reads it samples at `dt = 0`, **where the duration divides out of a time-normalized ease entirely** — a pop shorter than one frame passed unchanged. Both are now pinned by arithmetic, and a second battery of 10 targeted mutants killed 10/10. The strongest additions were the ones no fake bundle can make: the kick is ONE constant in authoring px applied to seven bodies whose drawn radii span 27.6-50 of them, so `rigComposition.test.ts` checks it against the shipped PNGs at the peak of the envelope — barrel-tip clearance holds at **1.61-1.86x** the drawn body radius (from 1.81-2.15x at rest), the housing never crosses the body centre, and the kick survives the scale down to world px (4.0 on a hero). Two more pin the DATA the design rests on as a tripwire: the enemy bundles ship no `attack` clip, and the hero's tracks `socket_r` and nothing else. See `design/01-rendering.md`, `design/12-art-animation.md` and `design/ROADMAP.md` "The gun never moved"
- [x] The wall swallowed the character and the pillar did not (2026-08-30, engine, `ENGINE_VERSION` 47): a report that arrived with its own oracle — *"房间中间的墙壁，能让角色只进入到墙壁的一半吗？"*, then, after a first answer reached for the wrong knob, *"你看房间里面的柱子，角色大概只有半个身子被柱子覆盖。能把墙也做成类似的效果吗？…这个截图里，角色整个跑到墙里面了。"* So the question was never "how much should a wall cover a character" but "why do the two standing things in one room disagree", which has an arithmetic answer. Everything here is drawn upward from a grounded origin (`screen.y = gy - z`), so a standing shape paints its footprint PLUS one drawn height of walkable floor to its north, and how deep a character sinks into that art is **drawn height minus the floor the shape reserves**. A pillar reserves `radius + solidRadius` = 48 px against 88.95 px of art and sinks a body **41 px** — covered to about the waist, the reading the report calls correct. An interior block reserved `solidRadius` alone, 16 px, against `WALL_H_INTERIOR` = 70 and sank it **54 px**, which is more than the whole silhouette at every size in the 20–48 px band. 13 px apart, and both numbers already pinned by existing tests — nothing had to be eyeballed. `config.WALL_NORTH_BRIM` (16 px = one body radius) closes it: `MovementSystem.resolveWalls` treats a wall carrying `AABB.freeStanding` as if its NORTH edge were that much further north, putting a block at **38 px**, within 2 px of the pillar. The rect never moves — drawn footprint, `blockedCells`, spawn placement, line-of-sight and the projectile query all still see the authored numbers. **One face** (v43 set east/west deliberately, and a body overlapping a wall's face from the SOUTH is the depth cue) and **flagged per solid, not given to every wall**: a perimeter ring is what door passages are carved through, and a room's south boundary is a 22 px kerb whose entire purpose is that a character CAN stand tangent to it — brimming that re-opens v43's *"太靠墙了，感觉陷进去了"* from the opposite side. **The cost was rasterized rather than argued**: standable floor over the whole launch map at 8 px, flood-filled, with and without — **45 connected regions either way, 38 rooms in the largest either way, 3.41% of the floor gone**. It removes floor and never a route; the seven gaps it narrows past the player are all pairs overlapping in x by exactly ONE cell, the diagonal notch at a corner, which you walk one cell aside and past. All three are assertions now, and raising the brim to two grid cells **fails the connectivity one** — the limit is real, 16 px is inside it. **The side effect is worth more than the fix**: the arena's deep pass — the fallback that drops a block's front FACE, the ugliest thing the x-ray does — went **1.07% → 0**, and it took four failing tests to notice. `needsDeepFade` fires on tall art over a shallow footprint, which on this map *is* the interior kit block, so all 778 firing samples were free-standing and the brim moves the player out of the band; the PvE floors keep theirs, whose deep cases are on perimeter runs. Those tests were not loosened but **replaced with the new fact against a control** — a bare `expect(0)` stays green when a sweep silently stops finding anything, so the zero is asserted against the same scan with the brim disabled, which must still find the old rate on `interior`-tier blocks. This supersedes the arena half of the 2026-08-27 entry above. **Battery: 5 mutants, all killed, and two of the tests did not exist until a mutant asked for them** — `subtractRect` dropping the flag killed nothing (a door only ever cuts a perimeter ring on the shipped maps, so the copy could be deleted with the whole suite green, surfacing the day a passage clipped a kit block as one block silently losing its brim), and the first bullet test could not have caught a brim leak into the projectile path because the geometry gave the same answer either way — its start position now lands one tick BETWEEN the brimmed edge and the real one. Also fixed: both 70k-sample coverage sweeps modelled "can the player stand here" with `solidRadius` alone and were sampling poses the engine no longer allows. **The LOOK is not verified** — driving the running game to walk a character into a wall failed (neither real nor synthetic key events moved the player, in a game that was visibly running), so the arithmetic is pinned and the frame is not; `38 px` is one constant away from any other answer. See `design/01-rendering.md` and `design/ROADMAP.md` "The wall swallowed the character and the pillar did not"
- [x] The wall widened, the monster joined the rule, and the loot stopped hiding in the wall (2026-08-30, engine, `ENGINE_VERSION` 48): live feedback on v47 itself, circled screenshot — *"角色被挡住的部分再上移一些，大概当前角色的一半可以进入墙。也就是现在感觉在1/2的位置，改为1/4的位置。怪物也要遵守同样的规则，截图中，角色根本无法拾取掉落的物品。"* Three changes, one report. **The brim widened from 16 to 23 px**, dropping an interior block's sink from 38 to 31 px — not the naive double to 32 that "half to a quarter" suggests: `launchArena.test.ts`'s route-connectivity sweep (flood-fill the shipped map's standable floor with/without the brim, assert the same rooms stay reachable) catches 32 px sealing a single-cell gap shut, 45 regions becoming 61. 23 is the actual ceiling on this content; going lower would leave floor on the table, going higher needs the room kits themselves given more spacing around a free-standing block, which is out of scope here. The wall/pillar sink PARITY v47 built is deliberately not preserved — nothing here touched the pillar's own reservation, so a wall now covers noticeably less of a character than a pillar does, left that way since nobody has reported the pillar's own version of this. **Every enemy blueprint now stops at its own body radius against a wall or pillar** (`content/enemies.ts`, `solidRadius: bp.footprintRadius` → `bp.radius`) instead of the smaller feet circle v43 deliberately left mobs on, on the grounds that widening it moves chase paths measured in `client/sim/pveLevelSim.sim.ts` — re-run against the new clearance and every balance gate still passes, including "nothing softlocks". **`geom.clampToWalkable` learned the same brim `MovementSystem.resolveWalls` already had** — this function (used to place a death drop, a weapon-swap's outgoing drop, and arena crate placement) had zero direct test coverage before this pass and was clamping a drop only against a free-standing block's bare footprint, never the brimmed band no actor's own collision would ever let them stand in. One honest finding while writing that test: at today's shipped numbers the single-block case never actually crossed into truly unreachable territory even pre-fix (the 31 px collect reach covers the 24 px worst-case gap) — the fix is still correct, and a numeric margin test now pins that relationship so a future widening of the brim can't silently erode it again; a compounding two-block corner case remains unchased. See `design/01-rendering.md`, `design/ROADMAP.md` and `engine/ENGINE_VERSION_HISTORY.md`'s v48 entry for the full account, including what this does NOT fix (a room-boundary/kerb wall's own worst measured coverage, 43.75%, is untouched — it fires below the x-ray's own trigger threshold and sits on geometry this constant never governs).
- [x] The buttons made no sound, and one press could not say no (2026-08-30, client + assets): a request in one line — *"现在我希望你能给ui也加上音效，比如按钮点击时"* — against a doc that had carried the slot since day one (`design/11`: *"UI-side cues … come from `10`'s ScreenManager, not the engine"*) and a client where nothing had ever filled it. Four cues, because a press has four possible answers: `ui.tap` (heard you), `ui.back` (you are leaving this screen), `ui.toggle` (the setting under your finger changed) and **`ui.denied`** — the one cue in the game that reports an *absence*, since an unaffordable craft or an ACQUIRE with nothing left to buy had been **indistinguishable from an input the game never received**. They are ordinary `AudioCue`s, so they inherit the catalogue, the voice cap, the sample-or-synth ladder and both backends unchanged; only the origin differs, and that is the whole design. `client/src/audio/uiSound.ts` is a **module sink attached at boot** rather than an `AudioBus` threaded through ~20 widget/screen classes that take no dependencies at all — the same shape `uiSkins`/`i18n` already use, with *unattached* as the safe state every widget test runs in. `Button` clicks by default (**48 buttons audible without opting in**), and the opt-ins are the ones that mean something else; the forge's craft rows and ACQUIRE go `'silent'` at the widget so `ForgeActions` can pick tap-or-denied from the transaction, because only the transaction knows whether the press did anything. Two details that are decisions, not accidents: the cue plays **after** the button's handler (the mute button applies the volume in its handler — reversed, muting would beep and unmuting would be silent), and a `Slider` ticks once on release **through the bus it just changed**, which is what makes a volume slider auditionable at all. Four more CC0 files (**50 shipped, 101.9 kB**) from the same Kenney pack `clash`/`wave-clear` came from, picked the opposite way round from the combat set: there was no incumbent voice, so the **sample was chosen first** — on `audit.py`'s `ui` gate plus the pack's own family names — and the synth voice was written to imitate its measured duration and centroid. `process_ui.py` is a separate driver for exactly one reason: a UI voice is a single `tone()`, so its peak IS its `gain` argument and needs no re-render to peak-match. **I cannot hear any of it, so I measured the bus**: 50/50 variants decoded in a real browser, a real mouse click on SETTINGS produced **one decoded 43 ms voice and zero oscillators**, real clicks on MUTE / CONTROLS / a slider release / BACK / an unaffordable craft / ACQUIRE each produced their intended cue, and peak PCM reads ui.tap 0.046 / ui.back 0.045 / ui.toggle 0.042 / ui.denied 0.057 against muzzle 0.055, impact 0.091, deflect 0.111 — the UI sits at or below the quietest combat cue, which is the mix `design/11` asks for. (An earlier 25 ms analyser poll read the same click as 0.004 and looked exactly like a broken cue; at 8 ms it is 0.046. The polling rate was the bug, not the audio.) **48 new/changed tests across 3 new files and 4 existing**, and **27/27 injected mutations caught** — two of which earned their keep: dropping the UI voice-cap priority from 110 to 10 survived the first round (nothing asserted that a press outranks a gunshot, so a saturation test now does), and the source sweep over all 48 `new Button(...)` sites is itself mutation-checked against going blind, because a regex that matches nothing passes everything. **Not verified**: the WeChat path on a real device, like the rest of that platform's audio.
- [x] The tests that were green while the fix did nothing (2026-08-30, engine + content, `ENGINE_VERSION` 49): not a bug report but a question — *"你有没有做逻辑同步的测试？比如修改墙壁或者柱子的阻挡范围，或者子弹的出生点，都可能导致逻辑不同步"* — and the honest first answer was one narrow parity test per past incident and no general mechanism. `design/18-test-strategy.md` is the resulting plan; building it found five real defects, the largest being that **the v47/v48 wall brim had been inert over the entire PvE campaign** (the constant was tuned against the launch arena, which flags every solid; all 14 shipped ember pieces carry none, and no test crossed the code/content line). Also: every determinism assertion in the repo compared two runs **in the same process**, which catches nondeterminism and nothing else — a committed golden-hash fixture now catches a replay-compatibility break, and the scenario that exercises the brim had to be a deliberately *held* direction, because a smooth orbit traces a circle and a circle is very good at going around things.
- [x] The rules were right and the bug was somewhere else (2026-08-31, engine + client, `ENGINE_VERSION` 50): third round of *"依然有掉落的物品无法拾取"*, this time with the reporter's own diagnosis attached. Both of their rules were **measured before being implemented**, and both already held — 903 drops across 16 bot-driven runs, zero unreachable, zero embedded in stone. They shipped anyway as constructions rather than margins (every mob's `solidRadius` floored at the player's, all three drop sites clamped by `dropClearance()`), together with four new gates and an honest note about which two of them actually discriminate. The report itself was **still unexplained** at the time, which was a result and not a resolution — *(superseded 2026-09-01: v51 found a mechanism that produces exactly this symptom, a locking door sealing a drop it closed over. v50's sweeps had measured the moment of the drop, never a resting place the wall set later moved under. See the v51 entry below.)*
- [x] A seed was never a repro, and nothing had ever recorded a run (2026-08-31, engine + client): asked whether recordings of that report existed, with the reason spelled out — *"单纯的起始种子还不够，因为怪物死亡时的位置和角色的位置有关"*. Correct, and the answer was worse than expected: the engine has been able to replay a match since Stage E and **nothing in `client/` or `server/` had ever called `toReplay`**. Now: recording is free and always on (`LocalInputSource` already retained every command), the **HUD's ● button or F9** saves the run marked at that tick, `?replay=<url>&pickupDebug=1` plays it back through the real renderer and holds at the mark, and `DD_REPLAY=<path> npm run replay:inspect` reports every drop's closest approach, swept path, gate and `pickup` event. Two traps the harness only revealed by being RUN: `PickupSystem` collects *during* a tick, so the one tick a drop was collectible is the one tick a post-step observer cannot see it (the first run called 7 of 8 drops "never collectible", four of which had been picked up), and a per-tick sample is not the closest the player's path came. A 48-mutant battery over the result killed 47 of 47 executed — and two of its first-round survivors were **redundant code, not missing tests**, including a doc comment that argued for the duplicate it was attached to.
- [x] The save verb gets a button, and the tests that were still missing (2026-08-31, client): *"你把f9的功能，做一个按钮放到界面上。然后看看有测试可以加吗"* — and the first half is the gap `HudView.pauseBtn` was built for in August, one layer over: a phone has no F9 key, and touch/WeChat is exactly the platform a bug report comes from. `HudView.replayBtn` ('●', beside the pause button) calls the same `saveReplay()` the keyboard does, **hides itself when the run cannot be saved** (online, or while watching a replay) rather than offering a dead control, and all three outcomes — saved / nothing to save / this device cannot — are localised across all eight locales, because a control pressed BECAUSE something went wrong must never report a false success. The tests half: the verb had unit coverage and **no coverage of its wiring**, which is exactly how `beginReplayRun` had shipped the day before with `this.engine` never assigned. `gameReplaySave.test.ts` drives a real `Game` with the browser's download seam faked and asserts the captured bytes replay to the live run's own hash; the battery grew to 48 mutants, 47 of 47 executed killed, and its one first-round survivor ("always claims success") is why the toast-text tests exist.
- [x] The game has music, after a month in which every piece of it existed (2026-08-31, client + assets + tools): two passes the same day, and the second one is the interesting one. **The first** produced the assets nobody had — `menu.mp3` (69.0 s) and `boss.mp3` (64.5 s), **1.09 MB**, cut from AI-generated Suno masters by the new `tools/audio-pipeline/process_music.py` and gated by a new `audit.py --class music`. CC0 was abandoned for music because CC0 music is almost entirely chiptune, a direct style mismatch for the flat-cel direction; both masters arrived at **-0.1 dBFS** against a cue set deliberately peak-matched down to **-14..-21**, so level is set by a band target (250-2000 Hz RMS at -30 dBFS, the band `impact`/`muzzle`/`ui.tap` all peak in), leaving every cue's peak 9-16 dB above the bed. That pass's own lesson was a method one: the region search and the acceptance gate **disagreed three times running** (3.01 vs 5.61 dB, 2.44 vs 3.39, 1.41 vs 3.69) because they were two implementations at different resolutions, weightings and processing stages — a ranking metric that is not the acceptance metric is a generator of near-misses, and the fix each time was to delete the second implementation rather than retune it. One master was also **prompted as a calm menu bed and measured as a sub-bass drone** (90% of its energy below 109 Hz), so it became the boss track instead of being thrown away. **The second pass — this one — is the runtime**, and the state it started from is worth naming because it is a failure mode rather than an oversight: *every individual piece existed and passed its own check.* The files were cut, measured and gated; `assetPacks.json` had a `music` subpackage rule; `settingsBinding.ts` computed the music volume and pushed it down on every slider drag; `design/11` had a locked decisions list; the whole suite was green. And the game was **silent**, because `setMusicVolume` was `(_v) => {}` in both backends and nothing ever opened either file. No unit test in the repo could have failed on that, because nothing was wrong with any unit. What shipped: `client/src/audio/musicCatalogue.ts` (an exhaustive `Record<MusicTrack, TrackDef>` — file, length, gain, and whether the file is really its own) and `MusicPlayer.ts`, whose whole design is that **the loop is a track change onto the same file**: `el.loop = true` is unusable because MP3 frame padding denies the sample-exact wrap every native looping API performs, so a second deck starts at `length - 2 s` and equal-power crossfades in. One envelope, both behaviours. The 2 s is not a taste choice — it is `audit.py`'s `XFADE_S`, the width of the window the shipped 1.15 dB / 1.63 dB seam figures were **measured across**, so a test now reads that constant out of the Python and asserts the two agree. The two platform decks are the only per-platform code and they differ structurally: web is element → `createMediaElementSource` → deck gain → **music bus** → destination, WeChat is an `InnerAudioContext` with **no audio graph at all**, so the settings volume has to be multiplied into each stream's own `.volume` alongside its fade — which is the answer to why `setMusicVolume`'s two implementations can never be shared. Music streams rather than decoding (a 69 s stereo loop is **~26 MB** of `AudioBuffer`), and on WeChat it is deliberately independent of `wx.createWebAudioContext`, the one audio API on that platform still unverified on the lowest base library, so a device without it loses the sampled cues and keeps the bed. `client/src/game/musicDirector.ts` **derives** the track from (phase, live state, our seat's room) every render frame instead of reacting to an event, which buys three things an event hook would each have to earn: no moment can be missed, nothing fires twice under prediction rollback, and **the autoplay gate crosses itself** — while the context is suspended the call has nothing to do, and the frame after the first gesture resumes it the same call starts the bed. Boss music keys off `RoomPiece.role === 'boss'` for our seat's room rather than off a live boss existing, so it changes as the door is crossed and stays changed over the death drops. **The third track has no master, and that is a field rather than a comment**: `dungeon.ember` plays `menu.mp3` with `TrackDef.borrowedFrom: 'menu'`, so which tracks are standing in is derived and asserted. It borrows `menu` and not `boss` even though `boss` is the closer match in mood — with one file on both sides of the boss-room threshold the switch would be **inaudible**, and "the music never switches" is indistinguishable from "the feature is broken"; a bed that is wrong for the room is a taste complaint, a transition nobody can hear is a bug report. Focus/blur is wired on **both** platforms for the first time on either (`visibilitychange` / `wx.onAudioInterruptionBegin`), and a held player also stops making loop-wrap decisions, since a deck paused ON the wrap point would otherwise fire the instant it was released. `Game.ts` was not touched: the tick sits in `GameLoop.update` ahead of the phase branch (so the MENU bed is driven at all) and reaches the device through a module sink, the `uiSound.ts` shape — plus one hard constraint, that the length gate pins `Game.ts` at its recorded size. **A blocker was settled before any code**: the WeChat main package had **2,729 bytes** of headroom and the runtime is code landing in `js/game.js`. Rather than assume WeChat bills the compressed package — the only authoritative source is an upload dialog that *publishes*, so that question is deliberately left open — `environment/door_curtain_raw.png` moved into a new bytes-justified `oversized` pack: **606 kB**, 468x832 of translucent gradient at 1.56 bytes per pixel, **12x the next door state** for one fixture. Main went to **3.42 MB / 4.00 MB**. Nothing was deferred on a reachability claim that would have been false, and the note says the real fix is a re-encode (a visual call a person has to make) and that the pack should be **deleted rather than refilled** when that lands. *(Both halves of that note are resolved as of 2026-09-01: the pack was deleted when the first download became code only, and the re-encode was then put to the owner as a render comparison at 1:1 device pixels and **declined** — the file stays at 468x832, because mean luma is flat across every downsample and only fine filament detail is lost, so the 606 kB buys texture rather than the cue itself.)* **The loops had had no TypeScript-side gate at all, and nobody had decided that**: `platform/audioAssets.test.ts` reads `public/audio/` **non-recursively**, so the moment music shipped into a subdirectory it fell out of that file's byte budget, credits cross-check, format check and licence sweep, silently and all at once. `audio/musicAssets.test.ts` is the replacement and three of its rules are **inversions** rather than copies — stereo is *required* where the cue gate requires mono (if those two assertions ever agree, one is broken); the catalogue length is checked against each file's real audible duration to 50 ms, because that number is where the crossfade is placed; and the AI masters' provenance is filed **outside** the CC0 path, since `packs.json` declares every SFX pack CC0 and a test asserts it of every entry. Its two remaining gaps — no archived licence text, no captured prompt — are now **declared and asserted false** rather than absent, because a test demanding they be filled would only invite them to be filled with a guess. **I cannot hear any of it, so I measured the stream**: in a real browser after the first gesture, `menu.mp3` arrives as a **206 Partial Content** range request (genuinely streaming, not fetched whole), one deck live reporting `duration: 69` exactly matching the catalogue, `loop: false`, deck gain rising 0.10 → 1.00 across 2 s of frames; `setMusicVolume(0.2)` moved the music bus to 0.2 and left the SFX bus at 0.5 and the deck fade at 1.0 — the slider that had gone nowhere for a month; switching to `boss` stopped deck 0 and streamed the second file at gain 1.0; zero console errors. One incidental finding is the position-read design paying for itself: in that embedded pane `requestAnimationFrame` is suspended while `document.hidden` stays *false*, so the entire game loop froze (0 frames in 3 s, measured) while audio kept streaming — the bed ran to its end, went quiet, and **the wrap fired on the first frame that ran**, because the wrap is read back from the stream rather than counted. A counter-driven wrap would have fired in the middle of nothing and never recovered. **~110 new tests across 6 new files; 66 injected mutations, 65 caught.** The three first-round survivors were each worth the trip: the web deck's `position()` had **no test at all** (returning 0 instead of null for an idle deck survived, because the player only reads the live deck today — the contract is what it is written against, so `webMusicDeck.test.ts` now exists), `ensureMusic`'s give-up latch was covered only on the *other* degrade path (so deleting it survived, and without it a construction that throws is re-attempted 60 times a second), and the third is a **genuine equivalent mutant** — deleting the biome→track lookup changes nothing, because the only biome that exists maps to `dungeon.ember`, which *is* the fallback. That one is recorded in the test as such, with the table-driven assertion that will start biting the moment a second biome loop ships, rather than being papered over with something that only looks like coverage. **Still open**: `dungeon.ember` has no master and whether `menu` wants re-generating is the owner's call; **nobody has listened** to the 2 loops or the 50 cues, which is the one thing here no measurement can close; the WeChat path is unverified on a device, now including whether `InnerAudioContext` accepts a path inside a loaded subpackage; and ambience plus the combat intensity layer are still design, both needing a stream the two-deck player deliberately does not have room for. See `design/11-audio.md` "The music runtime", `art/audio/README.md` "Music", and `design/ROADMAP.md` "Everything existed and nothing was connected".
- [x] The first download becomes code only (2026-09-01, client + build + docs): the previous day's byte crisis produced a question rather than a bug report — *"关于首包的问题，我觉得只需要代码和加载界面的图片即可。其他的都可以分批进行下载"* — and the answer is that the pack table shipped a week earlier had **bought nothing it was capable of buying**. `preloadCoreArt` called `ensureAllPacks()`: all six packs, at boot, in parallel. That satisfies WeChat's 4 MB rule (it is a rule about the FIRST download, and a pack fetched a moment later is compliant) and it was argued as such at the time, but every byte still arrived before the first frame, so art held **2.49 MB of the 4.00 MB** and every code change was taxed by what was left — which is how the music runtime, being code, found main at **2,729 bytes** of headroom and had to be paid for by moving a 606 kB PNG into a pack named for its own symptom. Now the main package is `js/game.js` and nothing else: **3.42 MB → 0.95 MB**, with `lobby` (`ui/*`, 0.38 MB) awaited at boot behind a progress screen and `music` / `forge` / `run` / the four unreachability packs downloaded in the background from the lobby. Total is unchanged at 5.72 MB — this moves bytes in **time**, and the headroom that matters became 3.05 MB of room for code. **The forge is the boundary, not START RUN**, because the forge is where a player *chooses* using weapon art; by then the background load has had the whole login/menu/mode-select sequence to finish, so the wait is almost always invisible. **The rule that keeps this from becoming a bug farm is that the set of available textures changes only at a phase boundary** — never per asset, never mid-room. Re-running a loader once its pack lands is free (all five are idempotent map-fillers over a static path table, and Pixi memoises by URL), but `RoomBuilder` builds a room's sprites **once**, so a texture arriving mid-room never reaches the sprite built without it: that is the "correct size, correct blend mode, default (0,0) position, green tests, live report" shape this codebase has the worst record with. So the 654 kB of art no authored content can even reach is awaited with the run phase anyway, rather than made lazy at its own point of use, because that would mean reasoning about a half-filled texture map. **`mainPack` and `defaultPack` stopped being one field** (`main` / `run`), and that flip is a safety property rather than bookkeeping: an asset added with no rule can no longer silently enlarge the first download, a rule is now required to opt *into* `main`, and a test sweeps the real `client/public` tree asserting nothing does. **Music is fetched and never awaited** — 1.09 MB, and the one asset class a game can start without — but it could not simply be backgrounded: a deck handed a path inside an unfetched subpackage plays nothing, and the per-frame director is a no-op once it believes the track is live, so the pack's completion calls the new `MusicPlayer.invalidate()`. One hook, at one place, that decides nothing and only forgets a failed answer; without it the menu bed would have been silent for the session, the exact regression the previous day's runtime pass existed to close. **Three things were found while building it, none of them in the plan.** (1) The kick has to happen *before* `new Game(...)`: `isRunArtReady()` answers true until something defers — which is what keeps the gate inert in every unit test — and `Game.start()` enters a run on its own first pass when `?replay=` is set, so a kick placed after `start()` lets that run begin with placeholder art and no gate at all; now pinned by reading both entry files' source. (2) **The progress bar could not have moved**: `ensureRunArt` memoised the promise and the background kick creates it with no listener, so the gate's callback arriving later was dropped — progress is a broadcast now, and a listener registering mid-download is immediately replayed the current count so a gate opening at 12 of 16 draws its bar at 12 instead of starting at zero and jumping. (3) **The build kept a full stale copy of the old main package**, and no gate could see it: the prune derived the directories to sweep *from the plan*, which works for a renamed texture and fails the moment a whole directory leaves main, and the byte gate weighs `client/public` through the rules rather than the built tree. The sweep is derived by exclusion now, and `build/wechatAssetSync.test.mjs` is new — its own first run then caught the rewrite throwing ENOENT on a fresh checkout, where this build is the one that creates the directory. **35 new tests across four new files**, the load-bearing one being `render/wechatPhasedBoot.test.ts`: it drives the real two-phase boot against the WeChat-shaped runtime (only `wx` is faked, backed by the real shipped files) and proves the run textures are *genuinely absent* after phase one rather than merely unused — the fake enforces WeChat's own rule that a file inside an unfetched subpackage names nothing — and then that the run phase fills every one of them in, which is the property the whole design rests on. **Verified live**: the lobby paints fully dressed with zero console errors, the forge shows every weapon card, a real run renders biome/rig/enemies/props, and the progress screen — built on the new `Layers.overlay` through the real renderer at 1280x720 — covers a live run and animates. That last check needed a screenshot to force frames: the in-app browser pane starves `requestAnimationFrame` when it is not compositing, and `ticker.lastTime` read 83 seconds stale while `FPS` still said 56 — the game was frozen too, which is what said the spinner was fine and the harness was not. **All three of the questions this shipped with were answered half an hour later in the real simulator** (2026-09-01, base library 3.17.1, design/04 item 17): the phased boot resolves every texture in both phases (17/17 UI after the lobby await, 7/7 rig bundles after the run phase) including packs that land ~850 ms into a live render loop; seven concurrent `wx.loadSubpackage` calls all succeed, so nothing is serialised; and the background download does not hitch the lobby, measured against two matched controls rather than eyeballed — the deferring runs delivered *more* frames at a lower mean and median, their only excess being two frames over 33 ms per 8 s window, in the first 1.7 s where the controls have long frames too. `LoadSubpackageTask.onProgressUpdate` is real and now typed, but the bar keeps counting units on purpose: it fires once per pack at `progress: 50` with an expected size of ~3.7 kB for payloads spanning 118 kB to 2.39 MB. **The traps were worth more than the results.** A first reading blamed `beginDeferredArt()` for 199 ms of main-thread block; removing the probe's own seven report writes from inside the timed span took it to **6 ms** — `writeFileSync` costs ~28 ms a call here, so a breadcrumb probe that flushes inside what it is timing fabricates exactly the hitch it went looking for. The before/after readability control turned out **void in the simulator** (a pack's files are readable *before* its `loadSubpackage`, because the whole project directory is served off disk), which retroactively narrows the 2026-08-25 claim that `wx.loadSubpackage` "works for real". And the first control run carried instrumentation the treatment runs did not — ~1 MB of synchronous reads inside the measured window — which briefly pointed the wrong way; both arms were re-run with identical probe builds before the numbers were believed. See `design/12-art-animation.md` "the first download is code only" and `design/04-wechat.md` "Package budget".
- [x] Two days of features, audited for what the tests did not say (2026-09-01, engine + client + build, `ENGINE_VERSION` 51): *"看看这两天加的功能，有没有测试可以加"* — asked against a suite that was **entirely green** (344 files, ~5,650 tests), so every finding below came from asking *"would this test fail if the behaviour were broken"* rather than from anything being red. Four parallel audits (music runtime, phased asset boot, replay recording, the v50 radius work), and **two of the findings were not test gaps at all.** (1) **A stray file in `platforms/wechat` crashed `npm run build:wechat`.** The prune derives its sweep set by *exclusion* from a `readdir` — deliberately, since a plan-derived set cannot clean up a directory the plan used to own, which is precisely what the previous day's asset move created — but exclusion also admits plain FILES, and each entry went straight to `walk()`, which readdirs whatever it is handed: `ENOTDIR ... scandir 'platforms/wechat/stray-note.txt'`. `platforms/` is git-ignored scratch that DevTools and a developer both write into, so this needed no contrivance; every existing prune test seeded only directories, so nothing looked. Pinned from both sides, and the second side matters more than it looks: the reserved `game.js` is *also* a file, and deleting the bundle entry every build is the failure mode a naive widening of that sweep reaches for. (2) **A locking door sealed the loot it closed over** — `ENGINE_VERSION` 51, and the mechanism v50 could not have found. `DoorSystem.rebuildWalls` is the only thing in this engine that changes the wall set mid-run; an item already lying in a passage was then inside stone, with nothing re-clamping it — nothing touches a pickup after its drop tick, and `PickupSystem` collects on a radius test that never consults walls, so reachability came down to whether a player's body could get within `pickupRadius + p.radius` of a point buried in a passage rect. A doorway is where fights happen, so the sequence is ordinary: a mob dies on the threshold, or a weapon is swapped while standing in one, and then the room activates. Written as a **failing test first**, then fixed by re-clamping every alive pickup at `dropClearance()` after `rebuildSpatialIndex()` — unconditional, because the "is this one affected" predicate is the same solid query `clampToWalkable` already performs and is exactly a no-op on a clear point, and idempotent, so a repeated rebuild cannot walk an item across the floor. **This is a candidate mechanism for the 无法拾取 report v50 closed as unexplained** (not confirmed as *the* report — that still needs a recorded run). **The most useful finding is why four rounds missed it**: v50's rules, and every gate built for them, are about the MOMENT OF THE DROP; *"is this drop site legal"* and *"is this resting place still legal"* are different questions. Worse, `design/18` claimed the v50 sweep re-checked every drop *"on every change to the wall set (so a door locking over an existing drop is covered)"* — the trigger was real and fired **142 times**, the case never once arose, and it therefore returned exactly the zero a working check returns while the bug was live. That sentence is corrected in place rather than having the fix quietly added beside it, and the lesson is now a rule: **a sweep's zero is only as strong as its evidence that the interesting sub-case actually occurred — count the sub-case, not just the samples.** **Then the backlog: ~30 gaps closed across 21 files (+1,592 lines)**, each verified by applying the named mutation, watching the test go red, and reverting — with three re-checked independently rather than taken on report. The headline ones had all survived the full suite: **`Game.beginReplayRun` had zero coverage** (the function that shipped broken the day before with `this.engine` never assigned, caught then only by live verification — re-applying that exact mutation now fails 4 tests); **`invalidateMusic` was untested on both backends**, an empty body surviving everywhere because every one under test was a hand-written fake, so nothing joined "the phased boot calls it" to "a deck actually re-points" — the same shape as the bug that had every music piece passing its own check while the game stayed silent; **`void ensureRunArt()` in `beginDeferredArt` was pinned by nothing**, and deleting it reverts the asset-phase feature's central claim (no background download, the full ~4.7 MB wait back at the forge); **the bundle's pack attribution was never exercised**, since no test ever wrote a `game.js`, so reverting to `defaultPack` survived while making the byte gate report the main package as 0 bytes against a 4 MB cap; and **`updateWeaponPickupPrompt`'s radius**, `nearbyWeaponPickups`'s only production call site, where moving the constant out of `HudView` had removed the duplicate *definition* and left the *use* unpinned — the surviving mutant silences the panel at exactly the distances 无法拾取 was reported at. **Three results worth more than the count.** (a) **Two existing tests claimed more than they pinned**, and one of them resisted the obvious fix: `assetManifest.test.ts` hand-copied the build's `packOf` instead of importing it, and *importing the real one changed nothing measurable*, because no shipped asset path matches more than one rule — `find`→`findLast` and `startsWith`→`includes` still both survived, and the stated claim ("first matching prefix wins") was unfalsifiable in either direction until an adversarial fixture table was added. De-duplicating an implementation out of a test removes a drift risk; it does not create discriminating power. (b) **`client/sim/dropReachability.sim.ts`** turns v50's ad-hoc 903-drop sweep — which was never committed — into a re-runnable gate (16 runs, 796 drops, 142 wall-set changes under live loot, 10,477 checks, ~6 s, `npm run test:drop-sim`, folded into `test:sims`), and it **says in its own header that it discriminates neither fix on today's content**: reverting v50's clamp gives byte-identical positions for all 796 drops, because every shipped room is authored on a 1000 fp lattice and that is exactly two player radii, and no door in those runs ever closed over a drop. It is a CONTENT gate, like the smoke pickup invariant it extends — its value is the next tighter room piece or wider body — and it carries a permanent oversized-probe assertion proving it is *capable* of failing here. Incidental measurement worth keeping: real drops rest **touching** stone, worst reach 1 fp, the truncation residue with no slack. (c) **Two guards were left uncovered rather than faked**: `preloadArt`'s listener-leak guards have no consequence observable from outside the module (`runArt` is memoised after resolve, so a retained listener can never fire again), and pinning them would mean adding an inspection seam to production code for a guard that is currently inert — the limitation is written into the neighbouring test's comment so the next reader does not mistake it for coverage. **Two gate-reading gotchas, both of which invert what the gate appears to say.** `goldenHash` passed *with the v51 fix applied and the version still at 50* — that, and only that, is the evidence the change moves no shipped scenario, because `serializeState` hashes `version` itself, so the instant you bump, **every** scenario's hash changes whether or not your change touches that path; run the hash gate BEFORE bumping or it tells you nothing. And a `ReferenceError: SIM is not defined` thrown from inside `DeathDropsSystem.tick` looked like a first-transform module-graph flake and was not: `SIM` does not appear in that file at all, so the only way to throw it there is to be executing while a *different* concurrent mutation check has temporarily rewritten the file to use `SIM.pickupRadius` without an import — parallel batteries in one working tree see each other's mutations, and an error naming a symbol the file does not contain means you are reading someone else's edit. Final state: `npm run check` green end to end — **349 test files, 6,275 tests**, plus typecheck, file-length and the WeChat byte gate. See `design/18-test-strategy.md` "What v51 found, in the sentence v50 wrote about it", `design/05-gameplay.md`, `engine/ENGINE_VERSION_HISTORY.md`'s v51 entry, and `design/ROADMAP.md` "Two days of features, audited for what the tests did not say".
- [x] The character gets an attack, and the sim finally says when (2026-09-02, engine + client + art, `ENGINE_VERSION` 52): a question rather than a report — *"角色现在有攻击动画吗？射击的和拿刀时的"* — whose honest answer was **half a shooting one, and nothing at all for a blade**. A shot played a procedural recoil envelope; a swing played nothing and *could not have*, because **the sim never told the render layer a swing happened** — `deflect` fires only when a swing catches a bullet and `hit` only when it connects, so a sword swung at empty air produced no event of any kind. Three different kinds of missing. **(1) The four enemy bundles had no clips to play** — `idle`/`hurt`/`death`/`spawn` only, no `move`, no `attack`. The missing `move` was a live bug on its own: `Actor` has always asked for `'move'` while an actor is moving and `playClip` resolves an unknown name to **no clip at all**, so a walking mob fell through to a bare rest pose and lost even its idle bob. All four gained both, plus the boss's own version in its shard rings (its weapon mount mode is `'none'` — the rings are its armament). **(2) The `attack` clips could not be played, structurally**: clips are sampled WHOLE and `playClip` swapped the clip outright, so playing `attack` dropped every bone it does not track back to rest for its whole duration. `render/rigClipLayer.ts` is the additive layer the 2026-08-30 note predicted would be needed — `rotation`/`translate` ADD, `scale`/`alpha` MULTIPLY, an unnamed bone comes through untouched — so `socket_l` keeps its hover bob through a shot and a `move` cycle keeps walking through an attack. It buys one authoring rule that no correct code can rescue, and it is a claim about DATA: **an attack clip must start and end at identity**, asserted per bundle. **(3) A blade cannot be swung by a clip, for two independent reasons**: a clip's `translateX` is applied in RIG space, so the hero's authored `-10` slid the gun *left* rather than back along its barrel — at exactly `RECOIL_MODULE_PX`'s magnitude, so the two would have doubled up (that translate is gone, and a test says it stays gone); and the weapon sockets are **aim-tracking bones** whose rotation `RigSkin` overwrites every frame, so an authored arc is discarded in silence. `rigRecoil.ts` became `rigAttackMotion.ts` and grew the melee half — cock 22° behind the aim, sweep to 46° ahead, recover to exactly the aim line, plus a forward lunge against the gun's backward kick, with `bodyPx` a single SIGNED number so the renderer keeps one formula. **The engine change is `melee_swing`**, the identical field list to `bullet_fired` so one reaction (`Actor.onAttack(kind)`) answers both. It could have been a render-side read of `weapon.justSwung`, which is already hashed sim state, and that would have been wrong: it is a **one-tick latch**, and `GameLoop.advanceOnline` drains every confirmed frame the server has ready and then reconciles the scene ONCE against the last of them — so every swing on an intermediate frame would be invisible, at a rate set by latency. **The bump is measured, and the measurement only works before it**: this engine serializes `version` into the hashed state, so bumping alone moves all five golden hashes and a post-bump run cannot tell "the event changed the sim" from "the version changed". Run against the **v51** fixture with the emit in place, all five hashes still matched and only the five `witness` assertions failed, each gaining one key — `melee_swing` at 7/27/36/44/67. Nothing diverges; the bump is what `versionContract.test.ts` demands before the re-record, and those counts are the first evidence the golden set reaches the melee path at all. **Two files went back under the 500-line convention rather than into the baseline** — `canonicalAimRad` moved to `facing.ts` where the mirror rule already lives (four call sites depended on it and none could reach it before; it has its own tests now) and the module-contact repaint to `rigShading.ts`, taking `RigSkin.ts` 500 → 481. **86 new test cases and 35 mutants across two batteries** (client 4221 / engine 1104). The first battery's own first run is the finding: it crashed inside the runner *after applying mutant #1 and before reverting it*, so every row printed after that was measured against a tree that was already failing — and every row still said KILLED. Re-run from a verified-green baseline with the revert in a `finally`, all 15 died, including the two only visible as *shape*: the melee lunge taking the recoil's sign (a character recoiling from their own sword) and the eye following the blade instead of the target. The second battery, 20 mutants over the coverage pass, had **three survivors, two of them real weaknesses in tests written minutes earlier** — a body-relative mount check that bounded a coordinate where `drawModuleContacts`' pull-in factor made the bound meaningless (now restated as an invariance), and a world-px readability check whose single `or` across three channels let every enemy translation shrink to 5% — a 2.72 wp lunge reduced to 0.14 wp — while coasting on the squash channel. The third survivor is a true equivalent, recorded as one: `advance`'s `Math.max(0, …)` clamp is unobservable because `progress` gates on `ms <= 0`. **Eight of that battery are DATA mutants**, edits to the shipped `animation.json` that no source-level test can reach — an attack clip that stops ending at identity, a loop seam that stops closing, `brute-core` drifting from the `critter-core` it is a hand-maintained copy of, a lunge authored past its own silhouette — and all eight die. **Also fixed in passing**: `build-critter-project.mjs` had drifted from the bundle it writes in two ways at once — re-emitting `body.png` from the raw 1254 px source over the shipped 256 px downsample (52 KB → 1 MB in the run pack) and re-deriving the binding scale against it, shrinking the critter to a fifth of its drawn size. Both were caught instantly by `rigComposition.test.ts`, which is the only reason this is a footnote and not a shipped regression; the shipped bundle is ground truth for both now. **Still open, deliberately**: `hurt`/`death`/`spawn` stay authored-and-unplayed (they need GameState signals `Actor` does not receive yet) *(superseded the same day — the entry below wires all three; the signals turned out to exist already, two of them as `GameState` diffs rather than events)*, a swing has no audio cue of its own, and the idle↔move base swap still has no cross-fade. See `design/12-art-animation.md`, `design/08-simulation-core.md`, `engine/ENGINE_VERSION_HISTORY.md` v52 and `design/ROADMAP.md` "The character gets an attack"
- [x] The other three clips finally get a signal (2026-09-02, client only): the follow-up the entry above listed as still open — *"继续把角色剩下的动作接上：hurt / death / spawn"*. All seven bundles ship all six clips and three of them had been played; these three never had, and **the reason was never the art — `Actor` received no signal**. All three already existed, so this needed no engine change: `hurt` is the `hit` event's `target` (`EventReactor` → `Actor.onHurt`), `death` is the id dropping out of the alive list and `spawn` is a new id appearing — both **diffs of `GameState`, not events**, so `Scene` drives them and `EventReactor`'s host interface deliberately does not grow them. **The layer each clip lands on was decided by the DATA**, checked across all three rig families before anything was written: `hurt` starts and ends at identity on all seven bundles, so it can be an additive overlay (and should be — a flinch must not interrupt a walk), while `spawn` opens at scale 0.2 / alpha 0 and `death` ends at scale 0.4×0.3 / translateY 18 / alpha 0, so **neither can be**: layered, a spawning body pops to 20% on the trigger frame and a corpse pops back to full size the instant its collapse finishes. So the base layer stopped being an idle/move pick and became a small state machine — **spawn → idle/move → death** — where a lifecycle clip outranks the caller's ground clip, `spawn` releases it back and `death` **holds its last frame**, because a corpse has nothing to return to. `Actor` still asks for `'idle'`/`'move'` every frame and never learns which one-shot is in flight. The one-shot overlay slot became a SET rather than a second named field, which is free: both channel operations are commutative, so two live overlays compose to the same pose in either order (asserted, since it is what would break if a channel became an override). **Death is absorbing** — it refuses later triggers and clears live overlays, so a corpse cannot flinch (written believing a killing blow could reach the view; the gap audit below found it cannot, and the claim was corrected rather than the rule removed). **Who owns the death** is the load-bearing boundary: the clip owns the body's own collapse, `ActorFilters` owns the dissolve shader *and* the clock that ends the view, so art gets no vote on view lifetime — and because the clip (900 ms) deliberately outlives `DISSOLVE_MS` (700), the corpse is always destroyed while still visibly collapsing, which is what makes the two read as one motion. **Three drawn marks had to learn to follow the body**, found by asking what `spawn`'s alpha and `death`'s scale actually reach: the **mounted weapon module** is a sprite, not a bone, so nothing faded it (a spawning character mounted a fully-opaque gun on an arm that had not arrived) — `ModuleMount` carries its mount bone's clip alpha now, joining the tether and the contact shade that already read it; and the **sphere shading** took position and alpha but never scale, fine for a 60 ms accent and not for a body collapsed to a third and *held* there for the whole dissolve, which read as a dark plate beside a shrinking corpse. **One real authoring bug fell out of the layer decision**, and it was invisible in every still: the three `char_*` bundles' `death` clip named `belly`/`socket_l`/`socket_r` **only in its final keyframe**, at alpha 0, and `sampleClip` resolves a bone with no keyframe at-or-before `t` straight to its earliest FUTURE keyframe — so a hero's belly and both weapon modules **vanished outright on frame one of the collapse** while the shell collapsed over the next 900 ms. Found by the identity assertion (`departure(death, 0)` came back 1 where the design said 0), not by looking; fixed with the eye's own alpha ramp, and the general rule is now asserted for every clip in every bundle: **every bone a clip animates must have a keyframe at `t = 0`**, or it cuts instead of ramping. **Verified live** through `window.__game` on a real offline run (the pane cannot present frames here, so the render TREE is the evidence, with the loop stepped by hand because a hidden pane throttles rAF): all **9 actors** in the first room read mid-materialise at once — body alpha 0.33-0.56, body `scale.y` 0.16-0.18 against a 0.39 rest, the sphere shade's alpha matching the body's to three decimals and the module's matching it exactly — and every one released 350 ms later to alpha 1 / shade 1 / module 1; a real `hit` produced **nothing on the trigger frame** then squash to 0.445/0.318 and alpha 0.62 **while the base clip's `y` kept marching −43.11 → −43.71**, which is the whole argument for the overlay in one measurement; and a real death collapsed `scale.y` 0.391 → 0.178, sank `y` −44.08 → −25.09 and faded to 0.387 with shade and module tracking, the view destroyed by the dissolve clock while the clip still read 0.39 alpha. Zero console errors. **65 new cases and 44 mutants, 43 killed on the first pass** (client 4324 green, `check:filelength` clean — nothing crossed 500 and nothing baselined grew). The single survivor was **a test of mine that was vacuous twice over**: the mutant moved `this.skin.spawn()` above the constructor's rest-pose measurement — which would centre the shield shell, hit outline, dissolve and health bar on a body at 20% scale *for the whole run* — and the test written to catch exactly that compared a number the clip cannot move on a fake bundle that **carries no clips at all**, so `spawn()` was a no-op and the two values matched trivially. Written by the same hand, in the same hour, as the argument against that shape. Both it and its `Skin` sibling now use a real spawn clip, read `filterArea`, and carry a second assertion proving the clip moves it; a re-run killed 2/2. **Ten of the 42 are DATA mutants** and all ten die — including **the death-clip pop re-injected**, which is the proof the new assertion catches the bug this pass found rather than merely coinciding with a green suite. **A gap audit afterwards** (*"有测试可以加吗"*) found six branches nothing reached and added **26 more cases (4350), with 11 targeted mutants and 11 killed — five of them failing exactly ONE test**: an overlay folded onto a LIFECYCLE base (every existing overlay case layered over `idle`), a zero-length frame with a lifecycle in flight, the degenerate zero-duration `hold` inputs, releasing into an unknown ground clip, `placeSphereShade`'s first direct unit tests (it was only covered through `RigSkin`, where its four properties can only be read together), the LOW quality tier — where there is no dissolve shader but the clip is not a shader, so a cheap tier must lose the erosion and keep the animation — and two DATA claims (`hurt`'s squash against the readability bar; the three `char_*` bundles being byte-identical, one rig with three sets of art written by one script that has drifted from its own output before). **The audit's real finding is an ordering fact nothing had stated**: `GameLoop` reconciles the scene BEFORE consuming the tick's events, on both paths — so on the tick an actor dies its view has already left `Scene.views` by the time the `hit` that killed it reaches `EventReactor`, and `actorAt` searches nothing else. A killing blow's flinch and hit-flash never land, and **`ClipLayers`' own "a corpse does not flinch" rule is unreachable from its one live caller.** Not a reason to delete the guard — a reason to stop describing it as holding the line: it is now tested as a property of the class that owns the rule, with all three facts that make it moot asserted separately (the order by invocation order on both paths, `actorAt` refusing a dying view, and the dying list still receiving its frame dt so the collapse plays). Two code comments were corrected for overstating what they knew. See `design/12-art-animation.md` and `design/ROADMAP.md` "The other three clips finally get a signal"
- [x] The four clips finally get a sound (2026-09-02, client + assets + tools): the entry above closed by naming what it had not done — *"a swing has no audio cue of its own, and none of `hurt`/`death`/`spawn` has one either"* — and the ask was *"把 hurt/death/spawn/挥击的音效补上，自己去找合适的音效。看看funny项目，他加了一些更广的找音效的网址"*. Four cues shipped (`swing`, `hurt`, `spawn`, `death.player`), plus a rename nobody asked for and the pass could not honestly avoid: **`death` became `death.enemy`**, because it had only ever been played inside `if (faction === 'enemy')`, so a player bleeding out — the moment a run ends — was **the one lifecycle event in the game with no sound at all**, and the union said nothing about it. `downed` stopped playing that same enemy explosion crunch for the local player collapsing and now plays `hurt`. **11 files, 20.9 kB; the set is 61 files / 122.7 kB** against a 160 KiB budget that was *not* raised to fit them. `spawn` is the only cue in the game with **no engine event behind it** — an id appearing in `GameState` is a diff, not an announcement, so `Scene.reconcile` reports how many actor views it built and `GameLoop` hands that count to `EventReactor` beside the frame's events, where a room's wave of nine bodies coalesces into one voice at higher gain exactly as ten hits do. Two findings outlive the cues. **The fixed CC0 pack inventory ran out for the first time**: there is no whoosh in any of the six Kenney packs' 323 files, and a fixed inventory cannot answer *"does this sound exist"* — only a queryable source can, which is the conclusion the sibling project `funny` had already reached from the other end. Its answer needs a credential this machine does not have, so daydayup got `tools/audio-pipeline/fetch_bigsoundbank.py` (CC0, no account) instead of a literal port — carrying across `funny`'s hard-won lesson in a different mechanism: **that search takes one noun**, `"sword whoosh"` returns zero where `"whoosh"` returns twelve, and it fails silently in a way that reads exactly like *"this sound does not exist"*. The useful material then turned out not to be the one-second files at all but the **eleven discrete sword swings inside a single 11 s take**, which measure far more homogeneously than four separate files could — which is what a variant set actually wants. **And spectral centroid is the wrong metric for a cue that has to survive a phone**: `hurt`'s first two candidate sets were body impacts chosen on centroid (594–873 Hz, entirely healthy-looking), and band-limiting each to 500–4000 Hz — what a phone speaker reproduces — showed they deliver **−48…−57 dBFS** there against the shipped `impact` set's −38.7…−39.7, with 96–98 % of their energy below 300 Hz. The game's single most important feedback cue would have been inaudible on the WeChat target while the cue it layers under was not, and the centroid had hidden it. What shipped (`impactGlass_light`, −32.5…−33.1) also closes a vocabulary on the way past: `shield.break` was already heavy glass, so the character's shell now has one material at two severities. Browser-measured rather than assumed: **61/61 variants decoded**, every new cue playing its *sample* and not its synth voice (zero oscillators) at `swing` 0.0555 / `hurt` 0.0786 / `spawn` 0.0589 / `death.player` 0.1007 against `muzzle` 0.0586, `impact` 0.0913, `deflect` 0.1087; and on a hand-stepped real run, `spawn:2` on the first frame, `impact:2, hurt:1` while the zone burned **both** seats (the local-seat gate, on live data), `swing:1` off a real `melee_swing`, and `death.player:1` on the local seat's own bleedout while an ally's identical death produced nothing. **30 new tests (4,350 → 4,380)** and a **39-row mutation battery over two rounds, all 36 real mutants killed** — round one's control was killed too, which is supposed to mean a broken harness and here meant the other thing it can mean: the control was mislabelled, because renaming one of a variable's three occurrences does not compile. One pre-existing defect was surfaced and deliberately left for its own pass: the frame that plays `death.player` also plays `win`, since `case 'win'` fires for any `win` event regardless of who won — a player who just died hears the victory sting. Still open, and unchanged in kind since the first sound shipped: **nobody has listened to any of the 61 files.** The specific thing to listen for now is the `hurt`/`impact` pair, which fire at the same instant and are separated by material and register rather than by envelope.
- [x] The swing shows the sector it actually hits (2026-09-02, client only, no engine bump): a two-part question — is the melee attack animation's amplitude tied to the weapon's own attack sector, and can there be an fx that shows that sector. The first answer was **no, they were unrelated numbers**: the roster's authored sectors run **60° (spear) to 220° (hammer)** against ONE hardcoded 68° sweep, so the spear's animation was wider than the sector it can hit in and the hammer drew 31% of its own — and since `DeflectSystem` parries bullets in that same `arcHalf`, the animation was misinforming the player about their own parry, not just under-selling a swing. Both halves now come from the weapon. `swingSchedule()` derives the travel (`arcDeg × 68/162`, clamped 26°–104° so a blade on an aim-tracking socket never sweeps through its own body) and the envelope (`recovery × 260/366.7`, clamped 130–400 ms), with **both factors defined as the ratio between the starter saber's shape and the constants hand-tuned against it**, so that weapon's look is unchanged by construction rather than by luck. The sector itself is drawn by `game/fx/slashArc.ts` — this renderer's only `Mesh`, because the look needs alpha varying radially *and* along the sweep at once and a `Graphics` carries only a one-dimensional ramp — sweeping the weapon's true `arcHalf`/`range`, resolved out of `GameState` so no event field, protocol change or `ENGINE_VERSION` bump was needed. Its brush is **generated, not prompted** (design/12's rule, second confirmed case): a 256×64 POT `bakedField` of a radial × tail alpha profile, parametrised on fractions so one bake serves every arc width, zero asset bytes. Verified by an A/B `extract` diff on a frozen live frame rather than by looking — the arc sweeps around a character already wearing a cyan shield shell, and no screenshot separates the two; that measurement (9,747 px lit, radii 71–125 against a geometric 46–119 + bloom, **159° of the hammer's 220° lit at once**) is also what drove the one tuning change, a tail exponent 3.2 → 2.4 after the first version measured 143°. **4,419 → 4,497 tests (+78)**, the one worth naming being `meleeArcParity.test.ts`: the sector was authored once and consumed twice with nothing cross-checking the two, so it drives the real `HitResolveSystem` and pins the drawn edge to `arcHalf` exactly, the drawn reach one target radius inside the hit boundary (so the lit edge always connects), and that the arc sweeps the same way the blade does in both facings. **A 39-row mutation battery killed 35**, and unusually all four survivors were real gaps rather than equivalents — three of them the same shape, *a test that reads one channel of a multi-channel buffer*: 19 cases had read the vertex buffer's sweep axis from every angle and never its radial one, never `indices`, and never whether either buffer was marked dirty. All four closed and re-killed.
- [x] The swing gets the hit window its own data always claimed (2026-09-02, engine + client, `ENGINE_VERSION` 53): a code question rather than a report, and the docs had already answered it. `MeleeSpec.swingSec` was authored on **all seven melee weapons** (0.1 s on the spear, 0.2 s on the hammer), carried the doc comment *"ACTIVE hit-window (subset of cooldownSec), 07 step 7"* and a row in design/09's conversion table — and **`toSimSpec` never converted it, `MeleeSimSpec` had no field for it, and nothing in the repo read it**. `HitResolveSystem.meleeArc` resolved the entire arc on the single tick `justSwung` latched, so every blade had a one-tick window and the hammer and the spear were identically timed. So the choice was implement-or-delete, and it was not really a choice: design/07 step 7 has specified the mechanic since it was written (*"a swing is active for a few ticks (`swingTicks`); during active frames, test the weapon's arc … each target is hit at most once per swing (track hit ids on the swing)"*). **Unimplemented spec, not dead data** — and the third time this exact `toSimSpec` gap has been found, after `MeleeSpec.knockback` (v25) and `RangedSpec.piercing` (v28). **Why nothing caught it:** every melee test in the engine staged a swing by hand-latching `w.justSwung = true` and asserting the hits on that same tick — which is precisely what the one-tick bug produces. 1104 tests were green over a mechanic that did not exist. What shipped: `MeleeSimSpec.swingTicks` (clamped into `[1, cooldown]`) plus three `WeaponState` fields — `swingTicksLeft`, `swingHitIds`, `swingDamage`. HitResolve and Deflect both gate on `swingTicksLeft > 0`; `justSwung` survives as the one-tick START latch. The arc **re-tests live facing and live positions every active tick** (a target that walks in is caught; a player who turns through their own swing carries the arc with them), while the **payload freezes on the start tick**, so a 6-tick hammer still deals its damage once and draws `combatPrng` once. `openSwing`/`closeSwing` are the only legal transitions, because a caller that set one field without the other produces a swing that silently does nothing — and six test setups were about to be exactly that caller. **Deflect shares the window deliberately**: design/07 locks *"the SAME arc both damages enemies and deflects bullets — there is no separate `blockArc`"*, and parrying for one tick while damaging for four is a separate blockArc in all but name. The parry window went **1 tick → 3-6 by weapon**, which is a real balance move on the pivot mechanic and is flagged as one rather than slipped in; it stands because the commitment design/05 asks for is spending the swing, and frame-perfect parry timing was not readable at 30 Hz. **The bump is measured, before bumping** (this engine serializes `version` into the hashed state, so bumping alone moves all five hashes): `deflect` up **5→7** and **1→3** — the feature working in shipped content — `melee_swing` counts **unchanged**, so the window did not become a second trigger, and **`prngCursors` byte-identical**, the load-bearing one, proving the crit is still exactly one draw per swing rather than one per active tick. `hit` moves in both directions and should: a bullet the wider parry catches no longer lands (62→61) while a body walking into a still-open arc now does (51→53). **The render half met a concurrent session head-on.** The entry above landed `swingSchedule` and `slashArc` hours earlier, having built the exact chain this needed — and having had to time it off **`recoveryMs`**, the gap until you may swing *again*, because `swingSec` was unconverted and there was no window to read. This branch's own client work was **structurally superseded and dropped before the merge** rather than resolved hunk by hunk; what remained was the thing the task had asked for all along. `SwingShape.recoveryMs` became `windowMs`, sourced from `swingTicks`, anchored so **`strikeEndMs` lands on the window's close** — the visible stroke now covers exactly the ticks that can connect (saber 260 → ~242 ms, hammer ~364, spear ~182), and the sector fx inherits all of it for free since it schedules off `strikeStartMs`/`strikeEndMs`. The two inputs are not even proportional across the roster (the spear spends 33% of its recovery active, the frostbrand 36%), so no constant could have stood in — a test pins the frostbrand/stormglaive envelope **ratio at 5/4 rather than 14/12**, so a silent revert to `recoveryMs` cannot pass. **One field was deleted rather than shipped**: an earlier draft put `swingTicks` on the `melee_swing` event, and `EventReactor.meleeSwinger` already resolves the whole spec out of the `GameState` every client holds — deliberately, with a comment saying so — which left the field with **no reader at all**. Shipping it would have created a second source of truth for a number its only consumer already had: the same authored-but-unread shape `swingSec` spent two months in, in the very pass that removed it. **33 new engine tests** (`systems/meleeWindow.test.ts`), each written to fail against a one-tick window — every one either counts the active ticks it observed or proves the target was unreachable on the swing tick before asserting it was reached later, because *"would this pass with `swingTicks: 1`?"* is the only bar that matters here. Includes `GameEngine.step()` end-to-end coverage (swings at ticks 2/13/24, active ticks `[2-5, 13-16, 24-27]`, an enemy parked in the arc sitting at 1 hp for all four ticks of the first window before the second kills it) and **four tests that perturb the new `serializeState` fields**, which nothing else in the repo could see — `replay.ts`'s own comment says a golden-replay comparison of two identical runs "never breaks" on a new always-equal field, so a hash addition is untested by construction, and all four mutants survived the entire repo before that block existed. **31 mutants, 31 killed** (21 engine, 10 render). Two lessons outlived the score. A **harness trap that faked a clean sweep of CRASHED**: on Windows `subprocess.run(text=True)` decodes with cp1252, which cannot decode the box-drawing characters vitest prints on FAILURE, so the reader threads die, stdout comes back **empty**, and every genuinely-killed mutant reads as a crash while the ASCII-only passing baseline stays green — the first run reported 21/21 crashed for that reason alone. And the one real survivor, worth the whole exercise: sampling the render envelope's `progress` against a **constant** rather than the current swing's own length is invisible mid-swing (the pose is at the wrong phase, but every "not at rest" assertion still holds) and shows **only at `u = 0`**, where the blade opens already cocked back — so for anything shaped like a curve, assert the endpoints and equal poses at equal FRACTIONS, not merely that it moved somewhere in the middle. Also fixed in passing: `meleeArcParity.test.ts` staged its seven-weapon probe with a bare `justSwung = true`, which opens no window any more, so it read as *the sector hits nothing* for every weapon. `npm run check` green on `main` after the merge (engine 1137, client 4499, server 189). `entities.ts` crossed 500 lines on the first draft of these doc comments and was **condensed back under rather than baselined**. Still open, deliberately: the parry widening wants a playtest, and design/07's "melee vs bullet-cloud cost" question got 3-6× more expensive — `DeflectSystem` still walks `state.projectiles` linearly, once per active tick per swinger, and has never used the broad phase. See `design/07-collision-combat.md` step 7, `engine/ENGINE_VERSION_HISTORY.md` v53 and `design/ROADMAP.md` "The swing that was one frame long"
- [x] Every door is the same door, whatever wall it is cut into (2026-09-03, client only, no engine bump): a screenshot of the shipped `forge → extraction` doorway with *“有些门会被墙盖住... 我希望门的表现是单独的，统一的，不管墙有多厚”* — some doors get covered by the wall. It read as an occluder bug and was not one: both wall-clips (`bordersDoorNorth` / `effectiveWallHeight`, 2026-08-20) were working and still fire. `doorFlankTier` gave a door the height of the SHORTEST run abutting its passage, and 11 of the 24 shipped doors are cut into a 22 px `WALL_H_KERB` — so they drew as a 128x22 letterbox under 64 px of **their own** cap stone, showing 12% of their own leaf art, while the other 13 drew a 64x104 portrait opening. The wall covering those doors was their own lintel. A door is a fixture, not a course of wall, so it now stands at one constant, `wallGeometry.DOOR_H` (= `WALL_H_PERIMETER`, which also keeps `MAX_WALL_HEIGHT` and the camera's framing pad correct by construction); `doorFlankTier` and `abutsAlongGap` are deleted rather than left unused. The cost is real and paid for: a door standing 104 px in a kerb covers ~82 px more floor, which the door's own x-ray has faded since the day it started standing — and the kerb itself is untouched, asserted as the control beside the height. Drawn WIDTH deliberately unchanged: an opening keeps the passage's own footprint, because a fixed aperture on a 128-wide gap would paint stone over walkable floor. Verified as a real frame (`renderer.extract`, before/after at identical framing) rather than by reading code. **38 distinct mutants, 38 killed** over two rounds: round one authored 34 and found 5 real survivors, four of them the `WEAR_ALPHA` shape this repo already knows — a layer whose GEOMETRY is covered and whose VALUE is covered by nothing (`OPEN_RECESS_ALPHA_TOP` set to the locked 0.72, and `SILL_ALPHA`/`GLOW_WASH_ALPHA`/`RIM_ALPHA` each to 0, all with every “present, ramps, right state” assertion green) — plus `RoomBuilder` handing the doors the WALLS' joins, invisible because no fixture had a door whose joins actually clip its cap. Two of the +5 new tests are assertion CLASSES the door suite lacked: how much of the leaf survives the crop (swept over all 24 doors against the shipped PNGs' real IHDR — it reports 12% and fails at the old height, making it the first test here that would have CAUGHT the report), and the x-ray genuinely firing at a kerb doorway (`simRenderParity.test.ts`, beside the kerb's own exemption: same rule, opposite verdict). That leaf test also caught a stale constant in `doorRender.ts`'s own header — the art has measured 147x217 since its margins were trimmed, not the 221x320 the header still claimed. 4534 → 4539 client tests. See `design/rendering/03-occlusion-and-doors.md` “Every door is the same door” and `design/roadmap/16-2026-09-03-doc-audit.md`.
