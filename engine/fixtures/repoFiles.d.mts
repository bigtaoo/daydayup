/** Types for `repoFiles.mjs` — deliberately free of any Node typing, see that file's header. */
export function readEngineFile(relPath: string): string;
export function engineSourceFiles(): string[];
